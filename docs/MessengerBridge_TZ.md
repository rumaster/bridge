---
title: Communication Platform with Programmable Automation and AI
subtitle: Enterprise Software Requirements & Architecture Specification
version: 1.1
status: Final Draft
authors:
  - OpenAI ChatGPT
customer: Confidential
language: ru-RU
---

# Communication Platform with Programmable Automation and AI

## Enterprise Software Requirements & Architecture Specification

**Version:** 1.1

---

# История изменений

| Версия | Дата | Автор | Описание |
|---------|------|--------|----------|
| 0.1 | Draft | Architecture Workshop | Первичная концепция |
| 0.5 | Draft | Architecture Workshop | Формирование общей архитектуры |
| 0.8 | Draft | Architecture Workshop | Интеграция AI Platform и FBP Engine |
| 1.0 | Final Draft | Architecture Workshop | Финальная редакция спецификации |
| 1.1 | Final Draft | Architecture Review | Внесены изменения по итогам ревью ТЗ (см. [`MessengerBridge_TZ_Review.md`](./MessengerBridge_TZ_Review.md)): устранены внутренние противоречия, включена программируемость в MVP, уточнена адаптация fbp-engine, добавлены разделы идентификации клиентов, инициализации организации, обработки ПДн внешних каналов, ёмкости и RPO/RTO. Реестр принятых решений — в Приложении В |

---

# Содержание

1. Введение
2. Назначение платформы
3. Цели проекта
4. Термины и определения
5. Архитектурные принципы
6. Общая архитектура платформы
7. Архитектура размещения платформы
8. Communication Core
9. Identity Platform
10. Integration Platform
11. Backend API
12. AI Platform
13. FBP Engine
14. Broadcast Platform
15. Notification Platform
16. SaaS Administration
17. Manager Workspace
18. Web Chat
19. Mobile API
20. Telegram Console
21. Frontend
22. Data Platform
23. Безопасность
24. Мониторинг и журналирование
25. Нефункциональные требования
26. Требования к тестированию
27. Требования к разработке
28. Требования к документации
29. Критерии приёмки
- Приложение А. Итоговая архитектура платформы
- Приложение Б. Размещение зарубежного кластера
- Приложение В. Открытые вопросы и принятые решения

---

# 1. Введение

## 1.1 Назначение документа

Настоящий документ определяет функциональные, архитектурные и нефункциональные требования к платформе **Communication Platform with Programmable Automation and AI**.

Документ предназначен для использования:

- архитекторами;
- Backend-разработчиками;
- Frontend-разработчиками;
- разработчиками AI;
- разработчиками баз данных;
- DevOps-инженерами;
- QA-инженерами;
- техническими руководителями проекта.

Документ является основным источником требований при разработке платформы.

---

## 1.2 Область применения

Communication Platform предназначена для организации омниканальной коммуникации между организациями и их клиентами.

Платформа обеспечивает единый механизм взаимодействия независимо от используемого канала связи.

Поддерживаемые каналы включают:

- Telegram;
- MAX;
- VK;
- WhatsApp Business;
- Web Chat;
- Mobile Application;
- Email;
- SMS.

Архитектура платформы предусматривает возможность подключения дополнительных каналов без изменения существующих компонентов.

---

## 1.3 Основная задача платформы

Основной задачей платформы является обеспечение надёжной доставки сообщений между организацией и её клиентами.

Все остальные функции платформы рассматриваются как вспомогательные.

В частности:

- AI повышает удобство использования;
- Broadcast автоматизирует массовые коммуникации;
- Notification информирует пользователей платформы;
- FBP Engine обеспечивает программируемую автоматизацию.

Ни одна из перечисленных функций не должна влиять на возможность обмена сообщениями при своей недоступности.

---

# 2. Назначение платформы

Communication Platform представляет собой единую SaaS-платформу, предназначенную для построения современных коммуникационных сервисов.

Платформа предоставляет организациям единый интерфейс для работы с клиентами независимо от используемых клиентами каналов связи.

Организация взаимодействует с платформой посредством:

- административной панели;
- рабочего места менеджера;
- Telegram Console;
- Backend API.

Клиенты взаимодействуют посредством:

- Web Chat;
- мобильного приложения;
- Telegram;
- MAX;
- VK;
- WhatsApp;
- Email;
- SMS.

Все каналы коммуникации рассматриваются как транспорт доставки сообщений.

Особенности конкретных каналов полностью инкапсулируются внутри Integration Platform.

---

# 3. Цели проекта

Платформа создаётся для решения следующих задач.

## 3.1 Единая коммуникационная среда

Все сообщения независимо от канала должны обрабатываться единым Communication Core.

---

## 3.2 Омниканальность

Клиент может использовать различные каналы связи.

Менеджер работает с единой историей коммуникаций.

---

## 3.3 Независимость от поставщиков

Особенности Telegram, WhatsApp, VK, MAX и других платформ не должны влиять на архитектуру Communication Platform.

Изменения API внешних сервисов должны локализоваться исключительно внутри соответствующих Adapter.

---

## 3.4 Программируемая автоматизация

Платформа должна обеспечивать возможность изменения поведения без изменения исходного кода.

Для этого используется FBP Engine.

Программируемая автоматизация является отличительной ценностью платформы и **входит в объём MVP**. В MVP администратору организации доступно не только включение/выключение и выбор версии заранее заданных Workflow, но и редактирование Workflow посредством визуального редактора схем, поставляемого в составе FBP Engine (см. §16.7). Возможности редактирования в MVP ограничены безопасным набором узлов (см. §13.13) и не требуют перекомпиляции платформы.

---

## 3.5 Использование искусственного интеллекта

AI используется исключительно как вспомогательная подсистема.

В платформе используются два независимых направления применения AI.

### AI Assistant

Используется при общении клиентов с организацией.

Основные функции:

- ответы на вопросы;
- использование базы знаний;
- генерация ответов менеджеру;
- поиск информации;
- перевод сообщений.

---

### AI Onboarding

Используется администраторами организаций.

Основные функции:

- первоначальная настройка платформы;
- помощь при подключении каналов;
- настройка автоматизации;
- изменение параметров организации;
- применение изменений посредством Backend API.

AI Onboarding не взаимодействует с клиентами организации.

---

## 3.6 Соответствие требованиям законодательства

Архитектура платформы должна обеспечивать возможность выполнения требований законодательства Российской Федерации посредством использования Edge Cluster.

В частности, обеспечивается локализация персональных данных граждан РФ по модели «первичная запись — в РФ, затем передача за рубеж» (RF-first), с первичной базой данных в РФ и трансграничной передачей в зарубежный Application Cluster с соблюдением требований 152-ФЗ. Требование детализировано в § 7.14 и § 23.9, обоснование — в Приложении В.

Архитектура должна оставаться единой независимо от региона размещения компонентов.

# 4. Термины и определения

Настоящий раздел определяет терминологию, используемую во всех последующих разделах документа.

Все участники проекта должны использовать данные определения при проектировании, разработке, тестировании и сопровождении платформы.

При возникновении неоднозначностей определения настоящего раздела имеют приоритет.

---

## 4.1 Platform

**Communication Platform** — единая программная платформа, обеспечивающая коммуникацию между организациями и их клиентами посредством различных каналов связи.

Платформа включает серверные компоненты, пользовательские интерфейсы, интеграционные сервисы, AI-подсистемы и программируемую автоматизацию.

---

## 4.2 Tenant

Tenant — отдельная организация, использующая платформу.

Каждый Tenant обладает собственной конфигурацией, пользователями, клиентами, настройками AI, подключёнными каналами и базой знаний.

Tenant является логически изолированной областью внутри платформы.

---

## 4.3 Organization

Термины Organization и Tenant являются эквивалентными.

В пользовательской документации рекомендуется использовать термин "Организация".

В технической документации допускается использование термина Tenant.

---

## 4.4 User

User — пользователь платформы.

Пользователь взаимодействует с административной частью платформы.

Роли в рамках организации (MVP):

- Administrator
- Manager

Роль уровня платформы (MVP):

- Platform Operator — оператор (владелец) платформы (super-admin), действующий вне контекста отдельной организации. Отвечает за провижининг организаций (Tenant), создание первого администратора организации и эксплуатационные функции SaaS. Подробно — §9.3 и §9.10.

Архитектура должна предусматривать возможность расширения набора ролей.

---

## 4.5 Client

Client — конечный пользователь организации.

Клиент общается с организацией посредством одного или нескольких каналов связи.

Клиент не является пользователем административной части платформы.

---

## 4.6 Communication Endpoint

Communication Endpoint — конкретный способ связи клиента.

Примеры:

- Telegram Account
- WhatsApp Number
- Email Address
- Phone Number
- Web Chat Session
- Mobile Application

Один Client может иметь несколько Communication Endpoint.

---

## 4.7 Channel

Channel — тип транспортного канала коммуникации.

Примеры:

- Telegram
- MAX
- VK
- WhatsApp Business
- Web Chat
- Mobile API
- Email
- SMS

Канал не содержит бизнес-логики.

Канал отвечает исключительно за передачу сообщений.

---

## 4.8 Adapter

Adapter — компонент Integration Platform, обеспечивающий преобразование данных между внешним API канала связи и внутренней моделью платформы.

Adapter отвечает исключительно за:

- получение сообщений;
- преобразование данных;
- отправку сообщений;
- обработку особенностей внешнего API.

Adapter не содержит бизнес-логики.

Adapter не принимает решений.

Adapter не выполняет маршрутизацию.

---

## 4.9 Communication Core

Communication Core — центральная подсистема платформы.

Communication Core отвечает за:

- приём сообщений;
- маршрутизацию;
- хранение;
- обработку;
- передачу сообщений прикладным сервисам;
- взаимодействие с Integration Platform.

Communication Core является главным компонентом платформы.

---

## 4.10 Message

Message — минимальная единица коммуникации.

Каждое сообщение независимо от происхождения после обработки Adapter должно иметь единый внутренний формат.

Сообщение может содержать:

- текст;
- вложения;
- служебные данные;
- ссылки на внешние объекты.

---

## 4.11 Conversation

Conversation — логическая история взаимодействия между организацией и клиентом.

Conversation объединяет все сообщения клиента независимо от используемого канала связи **при условии, что соответствующие Communication Endpoint связаны с одним Client** (см. §8.13 «Идентификация и объединение клиентов»).

Если клиент общается через Telegram, затем Web Chat и позже Email и эти Endpoint сопоставлены одному Client, все сообщения относятся к одной Conversation. До момента идентификации (в том числе для анонимных сессий, §18.6) сообщения ведутся в отдельной Conversation на неидентифицированный Endpoint.

Conversation является пользовательским представлением истории коммуникации.

---

## 4.12 Broadcast Campaign

Broadcast Campaign — массовая коммуникация, инициированная организацией.

Broadcast использует стандартный механизм доставки сообщений платформы.

---

## 4.13 Notification

Notification — внутреннее уведомление платформы.

Notification предназначено исключительно для пользователей платформы.

Notification не является сообщением клиента.

---

## 4.14 AI Assistant

AI Assistant — интеллектуальный помощник клиентов и менеджеров.

Основные функции:

- ответы на вопросы;
- работа с базой знаний;
- помощь менеджеру;
- поиск информации;
- генерация текста;
- перевод.

AI Assistant не изменяет конфигурацию платформы.

---

## 4.15 AI Onboarding

AI Onboarding — интеллектуальный помощник администратора организации.

Используется исключительно при настройке платформы.

Основные функции:

- подключение каналов;
- настройка организации;
- изменение конфигурации;
- применение изменений через Backend API.

AI Onboarding не взаимодействует с конечными клиентами.

---

## 4.16 FBP Engine

FBP Engine — программируемый Workflow Engine платформы.

FBP Engine используется для:

- исполнения AI Workflow;
- исполнения Automation Workflow;
- исполнения Business Workflow.

FBP Engine не является Message Broker.

FBP Engine не заменяет Communication Core.

FBP Engine выполняет только описанные схемами процессы.

---

## 4.17 Workflow

Workflow — исполняемый процесс, описанный средствами FBP Engine.

Workflow может включать:

- обращения к AI;
- вызовы Backend API;
- обработку данных;
- условия;
- циклы;
- маршрутизацию исполнения.

---

## 4.18 Knowledge Base

Knowledge Base — набор текстовых документов организации, используемый AI Assistant.

Каждый документ — это текстовая инструкция, вводимая администратором вручную (без загрузки файлов) и состоящая из двух полей: **название** (описывает суть документа) и **контент** (инструкция, встраиваемая в промпт при релевантном запросе). При сохранении документа для его содержимого вычисляется эмбеддинг и сохраняется в pgvector; далее AI Assistant подтягивает документ по семантическому поиску (RAG).

Knowledge Base не используется Communication Core напрямую.

---

## 4.19 Edge Cluster

Edge Cluster — региональный кластер платформы, предназначенный для обслуживания клиентских подключений.

Edge Cluster выполняет:

- приём подключений;
- обслуживание WebSocket;
- обслуживание Web Chat;
- обслуживание Mobile API;
- аутентификацию;
- выполнение требований законодательства РФ;
- буферизацию сообщений;
- управление VPN Tunnel Service.

Edge Cluster не содержит основной бизнес-логики платформы.

---

## 4.20 Application Cluster

Application Cluster — основной вычислительный кластер платформы.

Application Cluster содержит:

- Backend;
- AI Platform;
- FBP Engine;
- PostgreSQL;
- Broadcast;
- Notification;
- SaaS Administration;
- Manager Workspace;
- остальные серверные компоненты.

---

## 4.21 VPN Tunnel Service

VPN Tunnel Service — специализированный сервис защищённого обмена данными между Edge Cluster и Application Cluster.

VPN Tunnel Service обеспечивает прозрачную передачу сообщений между региональными кластерами.

---

## 4.22 Telegram Console

Telegram Console — дополнительный пользовательский интерфейс платформы.

Telegram Console предоставляет менеджеру возможность:

- получать уведомления;
- отвечать клиентам;
- просматривать сведения о клиентах;
- работать вне Web-интерфейса.

Telegram Console не содержит бизнес-логики.

---

## 4.23 Manager Workspace

Manager Workspace — основное рабочее место менеджеров организации.

Предоставляет единый интерфейс обработки всех коммуникаций независимо от канала связи.

---

## 4.24 SaaS Administration

SaaS Administration — административный интерфейс платформы.

Интерфейс разграничивается по уровню полномочий:

- **Уровень организации** (роль Administrator): управление одной своей организацией — пользователями, каналами связи, AI, Knowledge Base, Workflow, безопасностью и конфигурацией организации. Это основной сценарий MVP (§16).
- **Уровень платформы** (роль Platform Operator, §9.3): управление множеством организаций — провижининг Tenant, создание первого администратора, блокировка организаций, глобальная эксплуатация SaaS (§9.10).

Разграничение исключает неоднозначность «панель одной организации ↔ панель множества организаций»: это два разных набора полномочий одного интерфейса, а не противоречие.

---

# 5. Архитектурные принципы

Архитектура Communication Platform определяется следующими принципами.

Любое проектное решение должно соответствовать данным принципам.

При возникновении противоречий предпочтение отдаётся решению, наиболее полно соответствующему настоящему разделу.

## 5.1 Communication First

Главной задачей платформы является обеспечение надёжной коммуникации.

Если архитектурное решение не повышает качество коммуникации либо не упрощает сопровождение платформы, такое решение не должно включаться в MVP.

Все дополнительные функции рассматриваются как вспомогательные.

---

## 5.2 Единая платформа

Независимо от количества каналов связи, региональных кластеров, пользовательских интерфейсов и серверных компонентов платформа должна восприниматься как единая информационная система.

Пользователь не должен знать, какие компоненты находятся в Российской Федерации, а какие — за её пределами.

---

## 5.3 Простота

При наличии нескольких технически корректных решений предпочтение отдаётся наиболее простому.

Избыточное усложнение архитектуры не допускается.

Любое усложнение должно иметь измеримое обоснование.

---

## 5.4 AI как вспомогательная подсистема

AI не является ядром платформы.

Communication Platform должна полноценно функционировать при полном отсутствии AI.

Недоступность AI не должна препятствовать:

- приёму сообщений;
- доставке сообщений;
- работе менеджеров;
- работе клиентов;
- Broadcast;
- Notification.

AI повышает удобство использования, но не является обязательным компонентом коммуникации.

---

## 5.5 Программируемость

Поведение платформы должно определяться конфигурацией, схемами автоматизации и настройками организаций, а не изменением исходного кода.

Для реализации программируемого поведения используется FBP Engine.

FBP Engine позволяет изменять:

- сценарии автоматизации;
- бизнес-процессы;
- логику AI;
- последовательность обработки данных;
- правила принятия решений.

Изменение Workflow не должно требовать перекомпиляции платформы.

Программируемость поставляется уже в MVP: администратор организации может создавать и редактировать Workflow через визуальный редактор схем (§16.7), в пределах безопасного набора узлов (§13.13). Требование настоящего раздела и ограничение объёма MVP не должны противоречить друг другу.

---

## 5.6 Независимость каналов связи

Все каналы связи рассматриваются как транспорт доставки сообщений.

Communication Core не должен содержать зависимостей от конкретных каналов связи.

Особенности Telegram, MAX, VK, WhatsApp, Email, SMS и других платформ должны быть полностью инкапсулированы внутри соответствующих Adapter.

Добавление нового канала связи не должно требовать изменения Communication Core.

---

## 5.7 Расширяемость

Архитектура платформы должна предусматривать возможность расширения без изменения существующих компонентов.

Должна существовать возможность подключения:

- новых каналов связи;
- новых AI-моделей;
- новых Workflow;
- новых пользовательских интерфейсов;
- новых способов авторизации;
- новых внешних интеграций.

Расширение функциональности должно осуществляться посредством подключения новых компонентов, а не модификации существующих.

---

## 5.8 Независимость пользовательских интерфейсов

Все пользовательские интерфейсы работают исключительно через Backend API.

Пользовательский интерфейс не содержит бизнес-логики.

Все проверки полномочий, обработка данных и принятие решений выполняются серверной частью платформы.

---

## 5.9 Масштабируемость

Все серверные компоненты должны поддерживать горизонтальное масштабирование.

Увеличение количества экземпляров компонентов не должно требовать изменения архитектуры приложения.

Бизнес-логика должна оставаться неизменной независимо от масштаба развёртывания.

---

## 5.10 Приоритет сопровождаемости

При выборе между более сложным и более простым архитектурным решением предпочтение отдаётся тому, которое:

- проще сопровождать;
- проще тестировать;
- проще документировать;
- проще развивать.

Долгосрочная стоимость сопровождения является одним из основных критериев принятия архитектурных решений.

---

## 5.11 Архитектурная независимость AI

AI не имеет прямого доступа к внутренним компонентам платформы.

Любые изменения данных, инициированные AI, выполняются исключительно посредством Backend API.

Все действия AI должны проходить те же проверки безопасности, что и действия пользователей.

---

## 5.12 Единая модель данных

Независимо от количества каналов связи и пользовательских интерфейсов внутри платформы должна существовать единая логическая модель данных.

Все компоненты платформы работают с едиными сущностями.

Дублирование бизнес-сущностей между компонентами не допускается.

---

## 5.13 Разделение ответственности

Каждый компонент платформы должен иметь одну основную область ответственности.

Не допускается объединение нескольких независимых бизнес-функций внутри одного компонента без архитектурного обоснования.

---

# 6. Общая архитектура платформы

## 6.1 Общие положения

Communication Platform представляет собой модульную распределённую систему, предназначенную для организации омниканальной коммуникации между организациями и их клиентами.

Архитектура платформы разделена на независимые функциональные подсистемы, взаимодействующие посредством внутренних API и общей модели данных.

Каждая подсистема отвечает за ограниченную область ответственности и может развиваться независимо при сохранении совместимости с остальной платформой.

---

## 6.2 Логическая структура платформы

Платформа состоит из следующих основных подсистем:

- Edge Cluster;
- Application Cluster;
- Communication Core;
- Identity Platform;
- Integration Platform;
- AI Platform;
- FBP Engine;
- Broadcast Platform;
- Notification Platform;
- SaaS Administration;
- Manager Workspace;
- Web Chat;
- Mobile API.

Каждая подсистема имеет строго определённые обязанности.

---

## 6.3 Центральная роль Communication Core

Communication Core является центральным компонентом платформы.

Все входящие и исходящие сообщения проходят через Communication Core.

Ни одна прикладная подсистема не взаимодействует с внешними каналами связи напрямую.

Communication Core отвечает за:

- приём сообщений;
- маршрутизацию;
- сохранение сообщений;
- передачу сообщений прикладным сервисам;
- передачу сообщений в Integration Platform для доставки.

Communication Core не содержит логики AI.

Communication Core не зависит от конкретных каналов связи.

---

## 6.4 Прикладные подсистемы

Прикладные подсистемы используют Communication Core для решения специализированных задач.

К ним относятся:

- AI Platform;
- Broadcast Platform;
- Notification Platform;
- SaaS Administration;
- Manager Workspace.

Прикладные подсистемы не выполняют доставку сообщений самостоятельно.

Все сообщения проходят через единый механизм Communication Core.

---

## 6.5 AI Platform

AI Platform предоставляет интеллектуальные функции платформы.

В состав AI Platform входят два независимых направления:

AI Assistant

используется при взаимодействии менеджеров и клиентов.

AI Onboarding

используется администраторами организаций при настройке платформы.

Обе подсистемы используют единые сервисы AI, но имеют различные полномочия и различные сценарии использования.

---

## 6.6 FBP Engine

FBP Engine обеспечивает программируемое поведение платформы.

FBP Engine исполняет Workflow, описывающие:

- автоматизацию;
- сценарии AI;
- бизнес-процессы;
- обработку событий.

FBP Engine не принимает участие в транспортировке сообщений.

Обработка сообщений всегда остаётся обязанностью Communication Core.

---

## 6.7 Integration Platform

Integration Platform обеспечивает взаимодействие платформы со всеми внешними сервисами.

Каждый канал связи реализуется отдельным Adapter.

Adapter отвечает исключительно за преобразование данных между внешним API и внутренней моделью платформы.

Особенности внешних API полностью локализуются внутри Adapter.

---

## 6.8 Identity Platform

Identity Platform обеспечивает:

- аутентификацию пользователей;
- авторизацию;
- управление пользовательскими сессиями;
- управление ролями.

На этапе MVP используется вход посредством Telegram Bot.

Архитектура предусматривает возможность последующего подключения дополнительных механизмов аутентификации.

---

## 6.9 Broadcast Platform

Broadcast Platform предназначена для формирования массовых коммуникаций.

После формирования сообщений дальнейшая обработка полностью совпадает с обработкой обычной пользовательской переписки.

Broadcast Platform не взаимодействует напрямую с каналами связи.

---

## 6.10 Notification Platform

Notification Platform предназначена для информирования пользователей платформы о внутренних событиях.

Notification Platform не используется для коммуникации организации с её клиентами.

---

## 6.11 Пользовательские интерфейсы

Платформа предоставляет следующие пользовательские интерфейсы:

- SaaS Administration;
- Manager Workspace;
- Web Chat;
- Telegram Console;
- Mobile Application.

Все пользовательские интерфейсы используют единый Backend API.

---

## 6.12 Общая схема обработки сообщений

Жизненный цикл сообщения включает следующие этапы:

1. Получение сообщения Adapter соответствующего канала.
2. Преобразование сообщения во внутренний формат.
3. Передача сообщения в Communication Core.
4. Сохранение сообщения.
5. Определение дальнейшего маршрута обработки.
6. Передача сообщения прикладным сервисам при необходимости.
7. Формирование ответа.
8. Передача ответа в Integration Platform.
9. Преобразование ответа в формат соответствующего канала.
10. Доставка сообщения клиенту.

Независимо от канала связи последовательность обработки остаётся одинаковой.

---

## 6.13 Общая схема обработки команд AI

При использовании AI выполняется следующая последовательность:

1. Communication Core получает сообщение.
2. Backend определяет необходимость обращения к AI.
3. AI Platform получает запрос.
4. При необходимости AI использует Knowledge Base.
5. AI возвращает Backend результат или структурированную команду (§12.6); **AI не вызывает FBP Engine напрямую**.
6. При необходимости **Backend** инициирует Workflow в FBP Engine.
7. Workflow обращается к Backend API.
8. Backend выполняет проверки безопасности.
9. Backend изменяет данные платформы либо формирует ответ.
10. Communication Core доставляет ответ пользователю.

Инициатором Workflow всегда является Backend. AI не имеет прямого доступа к базе данных, не инициирует Workflow напрямую и не изменяет данные самостоятельно (§5.11, §12.5, §12.10).

Все изменения проходят через Backend API.

---

## 6.14 Независимость компонентов

Каждая подсистема должна иметь возможность независимого тестирования.

Изменение одной подсистемы не должно требовать изменения остальных компонентов при сохранении публичных контрактов.

Все взаимодействия между подсистемами должны выполняться посредством документированных API и внутренних контрактов платформы.

# 7. Архитектура размещения платформы

## 7.1 Общие положения

Communication Platform проектируется как единая распределённая информационная система.

Архитектура должна обеспечивать:

- выполнение требований законодательства различных стран;
- возможность регионального размещения компонентов;
- высокую доступность;
- масштабируемость;
- прозрачность работы для пользователей.

Распределённая архитектура является особенностью инфраструктуры и не должна влиять на бизнес-логику платформы.

---

## 7.2 Общая модель размещения

Архитектура MVP предусматривает использование двух основных региональных кластеров.

**Edge Cluster (Российская Федерация)**

Предназначен для обслуживания пользователей Российской Федерации.

**Application Cluster (зарубежное размещение)**

Является основной вычислительной площадкой платформы.

Архитектура должна предусматривать возможность подключения дополнительных региональных Edge Cluster без изменения прикладной логики.

---

## 7.3 Edge Cluster

Edge Cluster является региональной точкой входа пользователей.

Основные задачи Edge Cluster:

- приём клиентских подключений;
- обслуживание WebSocket-соединений;
- обслуживание Web Chat;
- обслуживание Mobile API;
- аутентификация пользователей;
- обеспечение выполнения требований законодательства РФ;
- управление VPN Tunnel Service;
- временная буферизация сообщений;
- обеспечение высокой доступности пользовательских подключений.

Edge Cluster не содержит основной бизнес-логики платформы.

Edge Cluster не содержит AI Platform.

Edge Cluster не содержит FBP Engine.

Edge Cluster не содержит административной панели.

---

## 7.4 Application Cluster

Application Cluster является основным вычислительным центром платформы.

В состав Application Cluster входят:

- Backend API;
- Communication Core;
- Identity Platform;
- AI Platform;
- FBP Engine;
- Broadcast Platform;
- Notification Platform;
- PostgreSQL;
- pgvector;
- SaaS Administration;
- Manager Workspace;
- сервисы мониторинга;
- сервисы аналитики;
- Integration Platform.

Все вычислительные задачи выполняются в Application Cluster.

---

## 7.5 Принцип единой платформы

Независимо от региона подключения пользователь работает с одной платформой.

Организация имеет:

- одну конфигурацию;
- один набор пользователей;
- одну административную панель;
- одну модель безопасности;
- единую историю коммуникаций.

Разделение инфраструктуры не должно приводить к разделению продукта.

---

## 7.6 Работа пользователей Российской Федерации

Пользователи Российской Федерации подключаются исключительно к Edge Cluster.

Edge Cluster выполняет:

1. установление соединения;
2. идентификацию пользователя;
3. проверку полномочий;
4. приём сообщений;
5. передачу сообщений в Application Cluster.

Пользователь не взаимодействует напрямую с зарубежной инфраструктурой.

Все особенности распределённой архитектуры полностью скрыты.

---

## 7.7 Работа зарубежных пользователей

Пользователи, не требующие подключения через Edge Cluster, могут обслуживаться непосредственно Application Cluster.

Архитектура должна поддерживать оба режима работы.

Выбор режима определяется настройками инфраструктуры.

---

## 7.8 VPN Tunnel Service

VPN Tunnel Service обеспечивает защищённую связь между региональными кластерами.

Все межкластерные соединения должны использовать:

- взаимную аутентификацию;
- шифрование;
- постоянный контроль соединения;
- автоматическое восстановление после разрыва.

VPN Tunnel Service является внутренним компонентом платформы.

Он недоступен пользователям.

VPN Tunnel Service находится на критическом пути Edge↔Application, поэтому для него определяются требования к ёмкости и деградации: целевая пропускная способность и запас по пиковой нагрузке, поведение при длительном разрыве (переход на буферизацию §7.9), контроль обратного давления (backpressure) при исчерпании ресурсов. Недоступность VPN Tunnel Service относится к деградируемым сценариям (§25.4) и не должна приводить к потере сообщений.

---

## 7.9 Буферизация сообщений

Если связь между Edge Cluster и Application Cluster временно отсутствует, Edge Cluster обязан продолжить приём сообщений.

Полученные сообщения помещаются во временное защищённое хранилище.

После восстановления соединения сообщения автоматически передаются в Communication Core.

Потеря сообщений вследствие временной недоступности канала связи не допускается.

**Ёмкость и поведение при исчерпании ресурсов.** Для буфера Edge Cluster определяются: предельная ёмкость (объём/число сообщений), TTL сообщений в буфере и стратегия при приближении к пределу (backpressure на приём, приоритизация, оповещение мониторинга). Дедупликация повторно переданных из буфера сообщений выполняется по сквозному `idempotency_key` (§11.12), а восстановление порядка — по порядковым номерам в рамках Endpoint (§7.10). Конкретные значения ёмкости и TTL задаются с учётом ориентиров нагрузки и RPO/RTO (§25.11).

---

## 7.10 Последовательность сообщений

Гарантия глобального порядка всех сообщений несовместима с горизонтальным масштабированием (§25.3) и повторными отправками at-least-once (§10.8, §11.12, §14.9), поэтому требование упорядочивания ограничивается уровнем **одной Conversation/Endpoint**.

Порядок сообщений сохраняется **в рамках одного Communication Endpoint (и, соответственно, одной Conversation)**: сообщения одного клиента по одному каналу должны поступать в Communication Core в том порядке, в котором они были приняты Edge Cluster. Между разными Conversation/Endpoint строгий взаимный порядок не гарантируется.

Механизм упорядочивания:

- **ключ партиционирования** — идентификатор Endpoint (либо Conversation); все сообщения одного ключа обрабатываются одной партицией/одним «писателем» (single-writer на партицию);
- **порядковый номер (sequence number)**, присваиваемый на входе Edge Cluster, позволяет Communication Core восстановить порядок и обнаружить пропуски;
- **дедупликация по сквозному `idempotency_key`** (§11.12) исключает дубли при ретраях и повторной передаче из буфера (§7.9).

---

## 7.11 Масштабирование регионов

Архитектура должна предусматривать возможность подключения дополнительных региональных Edge Cluster.

Добавление нового региона не должно требовать:

- изменения Backend API;
- изменения Communication Core;
- изменения AI Platform;
- изменения пользовательских интерфейсов.

Необходимыми являются только изменения инфраструктурной конфигурации.

---

## 7.12 Отказоустойчивость

Сценарии отказа региональных кластеров различаются по влиянию на платформу, и требования к ним формулируются раздельно.

**Отказ Edge Cluster.** Недоступность отдельного Edge Cluster не должна приводить к отказу всей платформы. Пользователи других регионов и пользователи, обслуживаемые напрямую Application Cluster, продолжают работу. При кратковременной потере связи Edge продолжает приём и буферизацию сообщений (§7.9), а при восстановлении выполняется автоматическая синхронизация накопленных данных.

**Отказ Application Cluster.** Application Cluster содержит основную бизнес-логику, БД и Communication Core, поэтому при его полной недоступности пользовательские операции не могут обслуживаться; Edge Cluster в этом случае обеспечивает лишь приём и буферизацию входящих сообщений (§7.9) до восстановления. Application Cluster является критическим для доступности платформы, и для него обязательна отдельная стратегия высокой доступности:

- размещение в нескольких зонах доступности (multi-AZ) одного региона;
- резервирование БД (реплики, автоматический failover) и сервисов ядра (несколько экземпляров);
- определение целевых RPO/RTO (см. §25.11);
- при необходимости — резервный регион (warm standby) для аварийного восстановления.

Таким образом, аптайм платформы определяется прежде всего доступностью Application Cluster, и требования к нему не должны подменяться более слабым сценарием отказа Edge Cluster.

При восстановлении работоспособности любого кластера должна автоматически выполняться синхронизация накопленных данных.

---

## 7.13 Размещение Adapter и WebSocket Gateway

Компоненты, непосредственно контактирующие с внешним миром, размещаются с учётом двух факторов: где физически находится противоположная сторона соединения и требований локализации персональных данных (см. § 7.14).

**WebSocket Gateway (подключения клиентов и операторов).** Обслуживание клиентских и операторских подключений (WebSocket, Web Chat, Mobile API) для пользователей Российской Федерации размещается в Edge Cluster (RF-контур), поскольку это точка первичного приёма данных от субъектов в РФ (см. § 7.3, § 7.6). WebSocket Gateway не содержит бизнес-логики и выполняет только приём/удержание соединений, идентификацию и передачу сообщений в Communication Core через VPN Tunnel Service.

**Adapter (интеграция с внешними каналами).** Adapter взаимодействует с внешними мессенджерами и сервисами (Telegram, VK, WhatsApp Business, Email, SMS и т. п.), серверы которых могут находиться за пределами РФ. Размещение Adapter определяется правилом:

- первичный приём и фиксация персональных данных субъектов РФ, поступающих через внешние каналы, должны происходить в RF-контуре (см. § 7.14) — то есть точка, в которой входящее сообщение впервые сохраняется, находится в RF-контуре;
- сетевое соединение с внешним сервисом (исходящий вызов к API мессенджера) может устанавливаться из того кластера, из которого этот сервис доступен, в том числе из Application Cluster;
- если Adapter технически должен работать из Application Cluster (например, из-за сетевой доступности внешнего API), первичная фиксация входящих сообщений всё равно выполняется в RF-контуре, а за пределы РФ передаются только данные в объёме и виде, допустимом моделью обработки персональных данных (см. § 7.14, § 23.9).

Таким образом, конкретное физическое размещение каждого Adapter является параметром инфраструктурной конфигурации, но правило «первичная фиксация ПДн субъектов РФ — в RF-контуре» соблюдается независимо от размещения.

---

## 7.14 Локализация персональных данных (152-ФЗ)

Настоящий раздел фиксирует принятое архитектурное решение по локализации персональных данных (ПДн) граждан РФ применительно к распределённой модели размещения (Edge Cluster в РФ, Application Cluster за рубежом). Решение принято по итогам анализа норм и практики (см. Приложение В).

### 7.14.1 Норма закона

Часть 5 статьи 18 Федерального закона № 152-ФЗ «О персональных данных» обязывает оператора при **сборе** персональных данных граждан РФ обеспечивать запись, систематизацию, накопление, хранение, уточнение (обновление, изменение) и извлечение этих данных с использованием баз данных, **находящихся на территории Российской Федерации**. С 1 июля 2025 года (Федеральный закон № 23-ФЗ от 28.02.2025) использование зарубежных баз данных для этих операций на стадии сбора прямо запрещено.

Ключевые следствия для архитектуры:

- **первичная (primary) база данных должна находиться в РФ**; зарубежная копия/реплика допустима, но первичная запись за пределами РФ является нарушением даже при наличии копии в РФ;
- **локализация и трансграничная передача — раздельные, кумулятивные обязанности**, задающие последовательность «сначала — запись в РФ, затем — передача за рубеж».

### 7.14.2 Принятое решение (RF-first)

1. **Первичный приём и запись ПДн — в RF-контуре.** Все входящие данные субъектов РФ (сообщения из внешних мессенджеров, Web Chat, Mobile API) первично записываются в первичную базу данных, размещённую в РФ (аттестованное по 152-ФЗ облако — например, Yandex Cloud, VK Cloud, Cloud.ru, Selectel классов защищённости УЗ-1/УЗ-2 — либо on-prem в РФ). Буфер Edge Cluster (§ 7.9) и первичная фиксация относятся к RF-контуру.
2. **Зарубежный Application Cluster работает с вторичной репликой.** Передача данных в зарубежный Application Cluster выполняется только **после** первичной записи в РФ; зарубежная БД является вторичной репликой, а не местом первичного сбора.
3. **Трансграничная передача — с уведомлением РКН (ст. 12).** До начала трансграничной передачи в зарубежный кластер оператор подаёт отдельное уведомление в Роскомнадзор. Для стран с адекватной защитой ПДн (участники Конвенции 108 и перечень по Приказу РКН № 128 от 05.08.2022) передачу можно начинать после подачи уведомления; для стран без адекватной защиты — только после решения РКН. Юрисдикция размещения Application Cluster должна выбираться с учётом этого перечня.
4. **Минимизация и обезличивание при передаче за рубеж.** За пределы РФ передаётся минимально необходимый объём данных. В зарубежные аналитические и вспомогательные подсистемы предпочтительно передавать **необратимо обезличенные (анонимизированные)** данные: только они выходят из-под действия 152-ФЗ. Псевдонимизация (обратимое обезличивание с сохранением ключа соответствия) **не** выводит данные из-под закона — такие данные остаются персональными (см. § 22.11, § 23.9).

### 7.14.3 Обязанности оператора по уведомлению

- **Уведомление об обработке ПДн (ст. 22)** — с указанием места нахождения базы данных (страны), что фиксирует соблюдение локализации; реестр операторов ведёт Роскомнадзор.
- **Уведомление о трансграничной передаче (ст. 12)** — до начала передачи (обязанность введена Федеральным законом № 266-ФЗ от 14.07.2022, действует с 01.03.2023).
- **Уведомление об инцидентах (утечках)** — в Роскомнадзор в течение 24 часов (первичное) и 72 часов (результаты внутреннего расследования).

### 7.14.4 Границы решения

Настоящее решение задаёт архитектурные требования (размещение первичной БД, последовательность «РФ → зарубеж», минимизация и обезличивание при передаче). Конкретная модель обработки ПДн, перечень собираемых полей, юрисдикция зарубежного кластера, тексты согласий и уведомлений определяются политикой обработки персональных данных оператора и подлежат правовой экспертизе на момент внедрения (см. § 23.9). Приведённые нормы и практика актуальны на дату ревизии документа и требуют перепроверки при внедрении.

---

# 8. Communication Core

## 8.1 Назначение

Communication Core является центральной подсистемой Communication Platform.

Все сообщения независимо от происхождения проходят через Communication Core.

Communication Core является единственной подсистемой, отвечающей за жизненный цикл сообщений.

---

## 8.2 Основные обязанности

Communication Core отвечает за:

- приём сообщений;
- регистрацию сообщений;
- маршрутизацию сообщений;
- сохранение сообщений;
- управление историей коммуникаций;
- передачу сообщений прикладным сервисам;
- передачу сообщений Integration Platform для доставки.

Communication Core не содержит AI.

Communication Core не содержит пользовательских интерфейсов.

---

## 8.3 Общая модель обработки

Каждое сообщение проходит следующие стадии:

Получение

↓

Проверка корректности

↓

Регистрация

↓

Сохранение

↓

Маршрутизация

↓

Обработка прикладными сервисами

↓

Отправка ответа

Все сообщения независимо от канала используют одинаковый жизненный цикл.

---

## 8.4 Единая модель сообщения

После обработки Adapter каждое сообщение должно иметь единый внутренний формат.

Минимально сообщение содержит:

- идентификатор сообщения;
- идентификатор организации;
- идентификатор клиента;
- идентификатор Endpoint;
- идентификатор канала;
- тип сообщения;
- содержимое;
- вложения;
- дату создания;
- служебные атрибуты.

Communication Core не должен зависеть от формата внешнего API.

---

## 8.5 Conversation

Communication Core объединяет сообщения в Conversation.

Conversation представляет собой полную историю общения организации с конкретным клиентом.

Все сообщения клиента, поступившие через связанные с одним Client Communication Endpoint, входят в одну Conversation (правила связывания — §8.13).

Переключение между Telegram, Web Chat, Email или Mobile Application не создаёт новую историю общения при условии, что соответствующие Endpoint сопоставлены одному Client. Для неидентифицированных (анонимных) Endpoint действует поведение по умолчанию, описанное в §8.13.

---

## 8.6 Маршрутизация

Communication Core определяет дальнейшую обработку сообщения.

Сообщение может быть направлено:

- менеджеру;
- AI Assistant;
- Broadcast Platform;
- Notification Platform;
- Automation Workflow;
- внешнему сервису.

Правила маршрутизации определяются конфигурацией платформы.

---

## 8.7 Независимость от AI

Communication Core не принимает решений на основе AI.

При необходимости использования AI Communication Core передаёт сообщение AI Platform.

При недоступности AI Communication Core продолжает работу.

---

## 8.8 Независимость от каналов

Communication Core не содержит информации о Telegram, MAX, VK, WhatsApp, Email или SMS.

Communication Core работает исключительно с внутренними сущностями платформы.

Все особенности конкретного канала локализуются внутри Adapter.

---

## 8.9 Передача сообщений

После формирования ответа Communication Core передаёт сообщение в Integration Platform.

Communication Core не взаимодействует напрямую с внешними API.

---

## 8.10 Журналирование

Communication Core фиксирует все этапы жизненного цикла сообщения.

Минимально регистрируются:

- время получения;
- источник;
- маршрут обработки;
- результат обработки;
- факт доставки;
- ошибки.

Журналирование является обязательной частью платформы.

---

## 8.11 Масштабирование

Communication Core должен поддерживать запуск нескольких экземпляров.

Экземпляры должны работать с единой моделью данных.

Добавление новых экземпляров не должно изменять алгоритмы обработки сообщений.

---

## 8.12 Архитектурный принцип

Communication Core является фундаментом платформы.

Все остальные подсистемы рассматриваются как прикладные сервисы, использующие возможности Communication Core.

Ни одна подсистема не должна дублировать функции Communication Core.

---

## 8.13 Идентификация и объединение клиентов

Требование единой Conversation по всем каналам (§4.11, §8.5) реализуемо только при наличии правил сопоставления разных Communication Endpoint одному Client (identity resolution). Настоящий раздел определяет эти правила.

**Поведение по умолчанию.** При поступлении сообщения с ранее неизвестного Endpoint Communication Core создаёт (или находит) Endpoint и связывает его с Client. Если Client определить нельзя (в том числе анонимный Web Chat, §18.6), создаётся отдельный Client/Endpoint и отдельная Conversation на эту сессию — до момента идентификации.

**Автоматическое связывание Endpoint с существующим Client** допускается только при подтверждённом совпадении верифицированного идентификатора:

- подтверждённый номер телефона (верификация одноразовым кодом);
- подтверждённый адрес электронной почты;
- явная привязка канала клиентом через код/ссылку связывания (например, переход в Telegram из авторизованного Web Chat).

Неверифицированные совпадения (совпадение имени, неподтверждённого телефона и т. п.) **не** являются основанием для автоматического слияния.

**Ручное объединение.** Менеджер (при наличии полномочий) может вручную объединить двух клиентов или перенести Endpoint между клиентами. Операция фиксируется в журнале аудита (§23.8) и должна быть обратимой либо сопровождаться сохранением истории изменений.

**Слияние историй.** При объединении клиентов их Conversation объединяются в единую историю с сохранением исходных меток канала и времени; порядок восстанавливается по времени и порядковым номерам (§7.10).

**Анонимный режим.** Для анонимных сессий действует одна Conversation на сессию. При последующей идентификации (авторизация, верификация контакта) сессия может быть присоединена к существующему Client по правилам автоматического или ручного связывания.

Все операции идентификации и слияния выполняются исключительно Backend и учитывают изоляцию организаций (§22.6).

---

# 9. Identity Platform

## 9.1 Назначение

Identity Platform обеспечивает идентификацию пользователей платформы, управление доступом и централизованную авторизацию.

Identity Platform является единственной подсистемой, отвечающей за аутентификацию пользователей и проверку их полномочий.

Ни один другой компонент платформы не должен самостоятельно реализовывать механизмы проверки прав доступа.

---

## 9.2 Область ответственности

Identity Platform отвечает за:

- аутентификацию пользователей;
- управление пользовательскими сессиями;
- управление ролями;
- проверку полномочий;
- регистрацию входов;
- отзыв активных сессий;
- аудит действий пользователей.

Identity Platform не отвечает за управление клиентами организации.

Конечные клиенты проходят идентификацию посредством соответствующих каналов связи и не являются пользователями административной части платформы.

---

## 9.3 Пользовательские роли MVP

В рамках MVP определяются две роли внутри организации (Administrator, Manager) и одна роль уровня платформы (Platform Operator).

### Platform Operator

Platform Operator — оператор (владелец) платформы, действующий вне контекста конкретной организации (super-admin). Полномочия включают:

- провижининг новых организаций (Tenant);
- создание первого Administrator организации и передачу ему доступа;
- блокировку/разблокировку организаций;
- эксплуатационные операции SaaS (мониторинг, управление глобальной конфигурацией платформы).

Platform Operator не участвует в повседневной работе организаций с клиентами и не имеет доступа к переписке организаций, кроме случаев, прямо предусмотренных политикой эксплуатации и законодательством. Действия Platform Operator фиксируются в журнале аудита (§23.8).

---

### Administrator

Administrator отвечает за управление организацией.

Полномочия включают:

- управление пользователями;
- подключение каналов связи;
- настройку AI;
- настройку автоматизации;
- управление Knowledge Base;
- управление Broadcast;
- настройку Notification;
- просмотр журналов аудита;
- использование AI Onboarding.

---

### Manager

Manager осуществляет ежедневную работу с клиентами.

Полномочия включают:

- обработку обращений клиентов;
- ведение переписки;
- использование AI Assistant;
- получение уведомлений;
- использование Telegram Console;
- просмотр истории коммуникаций.

Manager не имеет доступа к административной конфигурации платформы.

---

## 9.4 Архитектурная расширяемость

Архитектура должна предусматривать возможность последующего добавления новых ролей без изменения существующей модели безопасности.

Добавление новых ролей должно осуществляться посредством конфигурации и механизмов авторизации.

---

## 9.5 Способ аутентификации MVP

На этапе MVP используется аутентификация посредством Telegram Bot.

Процесс входа включает следующие этапы:

1. Пользователь вводит своё Telegram-имя.
2. Backend проверяет существование пользователя.
3. Telegram Bot отправляет одноразовый код.
4. Пользователь вводит полученный код.
5. После успешной проверки создаётся пользовательская сессия.

Одноразовый код имеет ограниченное время действия.

Повторное использование кода не допускается.

---

## 9.6 Дальнейшее развитие

Архитектура должна предусматривать возможность подключения дополнительных механизмов аутентификации без изменения пользовательских интерфейсов.

**Резервный способ входа для Administrator.** Вход в MVP выполняется через Telegram Bot (§9.5), что создаёт зависимость доступа к административной части от единственного внешнего мессенджера. Для роли Administrator должен быть архитектурно предусмотрен резервный, не-Telegram способ входа (например, email + одноразовый код), чтобы доступ к платформе не зависел от доступности и политики одного мессенджера. **В объём MVP реализация резервного способа не входит** — предусматривается только расширяемость модели аутентификации под его последующее подключение (провайдер входа как подключаемый компонент). Тот же механизм используется при первичной регистрации организации (§9.10).

В перспективе могут быть добавлены:

- резервный вход по email + одноразовый код (см. выше);
- OAuth;
- OpenID Connect;
- корпоративные SSO;
- аппаратные ключи;
- двухфакторная аутентификация;
- Passkeys.

Подключение новых способов входа не должно влиять на остальную архитектуру платформы.

---

## 9.7 Пользовательская сессия

После успешной аутентификации создаётся серверная пользовательская сессия.

Сессия содержит:

- идентификатор пользователя;
- организацию;
- набор ролей;
- срок действия;
- служебные параметры безопасности.

Истечение срока действия автоматически завершает пользовательскую сессию.

---

## 9.8 Авторизация

Перед выполнением любой операции Backend обязан проверить:

- наличие активной сессии;
- принадлежность пользователя организации;
- наличие необходимых полномочий.

Все проверки выполняются серверной частью платформы.

Frontend не должен самостоятельно определять доступность операций.

---

## 9.9 Аудит безопасности

Identity Platform обязана регистрировать:

- успешные входы;
- неуспешные попытки входа;
- завершение сессий;
- смену ролей;
- изменение прав доступа;
- административные действия.

Журнал аудита является обязательной частью платформы.

---

## 9.10 Инициализация организации (bootstrap)

Настоящий раздел определяет порядок создания новой организации и её первого администратора — сценарий, при котором в организации ещё нет ни одного пользователя, поэтому проверка «существования пользователя» (§9.5) неприменима.

Порядок инициализации:

1. Platform Operator (§9.3) выполняет провижининг организации (Tenant): создаётся изолированная область данных организации (§22.6) и базовая конфигурация.
2. Platform Operator задаёт первичный контакт первого администратора (Telegram-имя и/или email) и инициирует создание учётной записи Administrator.
3. Первому администратору направляется приглашение (одноразовая ссылка/код) для завершения регистрации и первого входа; при первом входе установление личности выполняется по этому приглашению, а не по проверке существующего пользователя.
4. После активации первый Administrator получает полномочия управления организацией и может приглашать остальных пользователей (Manager и др.).

Приглашение имеет ограниченный срок действия и одноразово. Все шаги инициализации (создание Tenant, создание первого Administrator, отправка и активация приглашений) фиксируются в журнале аудита (§23.8).

Само приглашение и первичная регистрация не должны зависеть от единственного внешнего мессенджера (см. резервный способ входа — §9.6).

---

# 10. Integration Platform

## 10.1 Назначение

Integration Platform обеспечивает взаимодействие Communication Platform со всеми внешними сервисами и каналами связи.

Integration Platform является транспортным уровнем платформы.

Она не содержит бизнес-логики и не принимает прикладных решений.

---

## 10.2 Основные задачи

Integration Platform отвечает за:

- получение сообщений;
- отправку сообщений;
- преобразование данных;
- обработку особенностей внешних API;
- управление подключениями;
- обработку ошибок взаимодействия с внешними сервисами.

---

## 10.3 Adapter

Каждый канал связи реализуется отдельным Adapter.

Adapter представляет собой независимый компонент Integration Platform.

Минимально Adapter реализует:

- получение сообщений;
- отправку сообщений;
- преобразование данных;
- обработку ошибок;
- контроль соединения.

Adapter не принимает бизнес-решений.

---

## 10.4 Поддерживаемые каналы MVP

Архитектура MVP предусматривает поддержку следующих каналов:

- Telegram;
- MAX;
- VK;
- WhatsApp Business;
- Web Chat;
- Mobile API;
- Email;
- SMS.

Добавление нового канала не должно требовать изменения остальных Adapter.

---

## 10.5 Независимость каналов

Communication Core не должен знать особенности конкретного канала связи.

После обработки Adapter каждое сообщение преобразуется в единую внутреннюю модель платформы.

Обратное преобразование выполняется перед отправкой сообщения клиенту.

---

## 10.6 Capability Model

Каждый Adapter обязан публиковать описание своих возможностей (Capabilities).

Capabilities определяют поддерживаемые функции конкретного канала.

Примеры возможностей:

- отправка текста;
- отправка изображений;
- отправка файлов;
- голосовые сообщения;
- видео;
- кнопки;
- реакции;
- индикатор набора текста;
- подтверждение прочтения;
- удаление сообщений;
- редактирование сообщений.

Communication Core принимает решения, опираясь на Capabilities Adapter, а не на название конкретного канала.

Это обеспечивает единый механизм поддержки различных платформ.

---

## 10.7 Работа с внешними API

Все обращения к внешним сервисам должны выполняться исключительно Adapter.

Другие подсистемы платформы не должны использовать API Telegram, WhatsApp, VK, MAX и других сервисов напрямую.

---

## 10.8 Обработка ошибок

При возникновении ошибок взаимодействия Adapter обязан:

- зарегистрировать ошибку;
- определить возможность повторной отправки;
- выполнить повторную попытку при необходимости;
- уведомить Communication Core о результате операции.

Обработка ошибок должна быть унифицирована независимо от используемого канала.

---

## 10.9 Масштабирование

Каждый Adapter должен поддерживать независимое горизонтальное масштабирование.

Увеличение нагрузки на один канал не должно влиять на работу остальных Adapter.

---

## 10.10 Архитектурный принцип

Integration Platform является транспортным уровнем системы.

Она обеспечивает обмен сообщениями между платформой и внешним миром.

Вся прикладная логика остаётся внутри Backend и Communication Core.

Таким образом достигается полная независимость бизнес-логики платформы от используемых каналов связи.

# 11. Backend API

## 11.1 Назначение

Backend API является единственной публичной точкой взаимодействия между пользовательскими интерфейсами, внешними интеграциями и внутренними компонентами платформы.

Любые операции чтения или изменения данных выполняются исключительно посредством Backend API.

Ни один клиентский компонент не имеет прямого доступа к базе данных.

---

## 11.2 Архитектурная модель

Платформа разделяется на **два уровня исполнения**, что снимает противоречие между «единым процессом развёртывания» и требованиями независимого масштабирования (§25.3) и отказоустойчивости подсистем (§25.4, §5.4).

**Уровень 1. Синхронное ядро (Modular Monolith).** Реализуется как модульный монолит на платформе **NestJS** и включает подсистемы с сильной транзакционной связностью: Identity Platform, Communication Core, доменные CRUD-модули (Organization, User, Client, Conversation, Message, Knowledge Base, Configuration, Audit) и Backend API как единую публичную точку входа. Для этого уровня действуют:

- отсутствие распределённых транзакций;
- единый процесс развёртывания;
- минимальная сложность сопровождения;
- простота разработки MVP.

Термин «единый процесс развёртывания» относится именно к синхронному ядру, а не ко всей платформе. Ядро масштабируется горизонтально запуском нескольких одинаковых экземпляров за балансировщиком (stateless-обработка запросов, состояние — в общей БД).

**Уровень 2. Вынесенные (деградируемые) сервисы.** Подсистемы, недоступность или перегрузка которых не должна влиять на обработку сообщений, выносятся из процесса ядра и вызываются через контракты (внутренний API/очередь): AI Platform, FBP Engine, Adapters (Integration Platform), WebSocket Gateway. Для них обязательны паттерны изоляции сбоев:

- таймауты на все внешние вызовы;
- circuit breaker и ограничение параллелизма (bulkhead);
- асинхронная постановка задач в очередь с ретраями;
- корректная деградация: при недоступности сервиса ядро продолжает приём, сохранение и доставку сообщений (§5.1, §5.4).

Архитектура должна предусматривать возможность дальнейшего выделения любого модуля ядра в самостоятельный сервис без изменения публичных контрактов API.

---

## 11.3 Основные модули Backend

В состав синхронного ядра Backend (Уровень 1, §11.2) входят следующие функциональные модули:

- Communication Core;
- Identity Platform;
- Organization Management;
- User Management;
- Client Management;
- Conversation Management;
- Message Management;
- Knowledge Base;
- Audit;
- Configuration.

Следующие подсистемы являются **вынесенными сервисами** (Уровень 2, §11.2) и интегрируются с ядром через контракты, а не как внутримодульные вызовы монолита:

- Integration Platform (Adapters);
- AI Platform;
- FBP Engine;
- Broadcast Platform;
- Notification Platform;
- WebSocket Gateway.

Для взаимодействия с вынесенными сервисами Backend содержит тонкие модули-интеграции (например, FBP Engine Integration, AI Integration, Integration Gateway), инкапсулирующие вызовы контрактов, таймауты и обработку деградации.

Каждый модуль имеет собственный слой бизнес-логики.

---

## 11.4 Внутреннее взаимодействие модулей

Модули взаимодействуют посредством внутренних сервисных интерфейсов NestJS.

Прямой доступ одного модуля к внутренним данным другого модуля запрещён.

Каждый модуль предоставляет только документированные публичные интерфейсы.

---

## 11.5 Внешние интерфейсы

Backend предоставляет следующие категории API:

- REST API;
- WebSocket API;
- Internal Service API.

REST API используется пользовательскими интерфейсами и внешними системами.

WebSocket используется для событий реального времени.

Internal API используется исключительно внутренними компонентами платформы.

---

## 11.6 REST API

REST API является основным способом взаимодействия с платформой.

Каждый ресурс должен иметь единообразный набор операций.

Минимально поддерживаются операции:

- создание;
- получение;
- изменение;
- удаление;
- поиск;
- фильтрация;
- постраничная навигация.

Все операции должны быть идемпотентными там, где это возможно.

---

## 11.7 WebSocket API

WebSocket используется исключительно для событий реального времени.

Минимально должны поддерживаться следующие события:

- получение нового сообщения;
- изменение статуса сообщения;
- начало набора текста;
- окончание набора текста;
- изменение статуса клиента;
- получение уведомлений;
- изменение состояния Broadcast;
- изменение состояния Workflow.

Соединение должно автоматически восстанавливаться после временной потери связи.

---

## 11.8 Версионирование API

Все публичные API должны поддерживать версионирование.

Версия API указывается в URL.

Изменение существующей версии API, нарушающее обратную совместимость, не допускается.

Все несовместимые изменения публикуются только в новой версии API.

---

## 11.9 Формат данных

Все API используют JSON.

Идентификаторы объектов представлены UUID.

Дата и время передаются в формате ISO-8601 UTC.

Все текстовые данные кодируются в UTF-8.

---

## 11.10 Валидация

Все входящие запросы проходят обязательную серверную проверку.

Проверяются:

- обязательные поля;
- типы данных;
- ограничения длины;
- допустимые значения;
- принадлежность данных организации;
- права пользователя.

Недопустимые запросы не должны достигать бизнес-логики приложения.

---

## 11.11 Обработка ошибок

Все ошибки возвращаются в едином формате.

Ответ об ошибке должен содержать:

- код ошибки;
- краткое описание;
- человекочитаемое сообщение;
- идентификатор запроса;
- дополнительную диагностическую информацию (при необходимости).

Формат ошибок должен быть единым для всех модулей платформы.

---

## 11.12 Идемпотентность

Операции создания сообщений, Broadcast и других объектов должны поддерживать механизм защиты от повторной обработки.

Повторная отправка одинакового запроса не должна приводить к созданию дублирующихся данных.

**Сквозной ключ идемпотентности.** Для сообщений вводится сквозной `idempotency_key` (совпадающий с `message_id`), генерируемый в точке входа (клиент/Edge Cluster) и сохраняемый на всём пути доставки: клиент → Edge → буфер (§7.9) → Communication Core → Adapter → внешний канал. На каждом переходе повторная передача сообщения с уже обработанным ключом отбрасывается (дедупликация), что делает совместимыми буферизацию (§7.9), повторные отправки at-least-once (§10.8, §14.9) и упорядочивание по Endpoint (§7.10). Ключ хранится до подтверждения доставки и в течение окна дедупликации.

---

## 11.13 Логирование

Backend обязан регистрировать:

- входящие запросы;
- исходящие ответы;
- ошибки;
- исключения;
- длительность обработки;
- идентификатор пользователя;
- организацию;
- идентификатор запроса.

Журналирование должно быть единообразным для всех модулей.

---

## 11.14 Документирование API

Все REST API документируются посредством OpenAPI (Swagger).

Документация должна генерироваться автоматически на основании исходного кода.

Каждое изменение API должно автоматически отражаться в документации.

Документация является частью поставляемого продукта.

---

# 12. AI Platform

## 12.1 Назначение

AI Platform предоставляет интеллектуальные функции платформы.

AI Platform является вспомогательной подсистемой.

Недоступность AI Platform не должна препятствовать работе Communication Platform.

Все критически важные функции платформы продолжают работать независимо от состояния AI.

---

## 12.2 Основные компоненты

AI Platform состоит из двух независимых направлений:

- AI Assistant;
- AI Onboarding.

Обе подсистемы используют общую инфраструктуру работы с языковыми моделями, но имеют различное назначение и различные полномочия.

---

## 12.3 AI Assistant

AI Assistant предназначен для помощи менеджерам и конечным клиентам.

Основные функции:

- ответы на вопросы клиентов;
- поиск информации;
- использование базы знаний;
- генерация вариантов ответов;
- перевод сообщений;
- краткое резюмирование переписки;
- помощь менеджеру при подготовке ответа.

AI Assistant не изменяет конфигурацию платформы.

AI Assistant не выполняет административные действия.

---

## 12.4 AI Onboarding

AI Onboarding предназначен исключительно для администраторов организации.

Основные функции:

- первоначальная настройка платформы;
- подключение каналов связи;
- настройка автоматизации;
- управление Knowledge Base;
- изменение параметров организации;
- помощь при конфигурировании системы.

Все изменения, инициированные AI Onboarding, выполняются исключительно через Backend API после прохождения всех проверок безопасности.

---

## 12.5 Принцип работы

AI Platform не взаимодействует с базой данных напрямую.

Последовательность обработки запроса:

1. Backend принимает запрос.
2. Backend формирует контекст.
3. AI получает только необходимый контекст.
4. AI формирует ответ или структурированную команду.
5. Backend проверяет результат.
6. Backend выполняет необходимые операции через собственную бизнес-логику.
7. Пользователь получает результат.

Таким образом, AI никогда не обладает полномочиями непосредственного изменения данных.

---

## 12.6 Структурированные ответы

При необходимости выполнения действий AI возвращает структурированный JSON-документ.

JSON содержит описание требуемой операции и её параметры.

Backend валидирует полученную структуру, проверяет полномочия пользователя и только после этого выполняет соответствующие действия.

Никакие SQL-запросы, команды или программный код, сформированные AI, не исполняются напрямую.

---

## 12.7 Knowledge Base

AI Assistant использует базу знаний организации.

Knowledge Base представляет собой набор документов, предоставленных организацией.

Документы индексируются с использованием pgvector.

AI использует поиск по смысловому сходству (semantic search) для формирования контекста ответа.

Knowledge Base является логически изолированной для каждого Tenant.

---

## 12.8 Использование FBP Engine

Все сложные сценарии работы AI реализуются посредством FBP Engine.

FBP Engine определяет:

- последовательность вызовов моделей;
- обработку результатов;
- взаимодействие с Backend API;
- использование внешних сервисов;
- маршрутизацию данных.

Изменение логики AI должно выполняться изменением Workflow, а не изменением исходного кода платформы.

---

## 12.9 Использование языковых моделей

Архитектура не зависит от конкретного поставщика LLM.

Должна существовать возможность подключения различных моделей без изменения прикладной логики платформы.

Выбор конкретной модели определяется конфигурацией организации или администратора платформы.

Поддержка нескольких моделей одновременно должна быть предусмотрена архитектурно.

---

## 12.10 Изоляция AI

AI Platform не должна иметь прямого доступа:

- к PostgreSQL;
- к внутренним сервисам;
- к файловой системе;
- к настройкам платформы;
- к пользовательским сессиям.

Все обращения выполняются только через документированные Backend API с обязательной проверкой полномочий.

Это обеспечивает предсказуемость, безопасность и возможность аудита всех действий, инициированных искусственным интеллектом.

# 13. FBP Engine

## 13.1 Назначение

FBP Engine является встроенным программируемым Workflow Engine платформы.

В основе подсистемы используется проект **fbp-engine**, адаптированный под архитектуру Communication Platform.

FBP Engine обеспечивает возможность изменения поведения платформы без изменения исходного кода Backend.

FBP Engine не является ядром платформы.

Он выполняет исключительно сценарии, инициированные Backend.

---

## 13.2 Архитектурная роль

FBP Engine располагается между прикладной логикой платформы и AI Platform.

Он используется как исполнитель бизнес-процессов, сценариев автоматизации и AI Workflow.

Communication Core никогда не зависит от FBP Engine.

При недоступности FBP Engine обмен сообщениями продолжает работать.

---

## 13.3 Основные задачи

FBP Engine используется для реализации:

- AI Workflow;
- Business Workflow;
- Automation Workflow;
- сценариев обработки событий;
- последовательностей вызова внешних сервисов;
- интеллектуальной автоматизации.

FBP Engine не используется как Message Broker.

FBP Engine не маршрутизирует сообщения.

---

## 13.4 Основные сущности

В архитектуре платформы используются следующие логические сущности FBP Engine.

### Workflow

Исполняемый процесс.

Workflow определяет последовательность выполнения операций.

---

### Node

Минимальная исполняемая единица Workflow.

Каждый Node выполняет одно действие.

Примеры:

- вызов LLM;
- обращение к Backend API;
- поиск документов;
- обработка текста;
- ветвление;
- ожидание результата.

---

### Connection

Определяет направление передачи данных между Node.

Connection образует граф исполнения Workflow.

---

### Execution Context

Набор данных, доступных во время исполнения Workflow.

Execution Context может содержать:

- параметры пользователя;
- данные организации;
- идентификаторы объектов;
- результаты предыдущих Node;
- временные вычисления.

Execution Context существует только во время выполнения Workflow.

---

### Variables

Workflow может использовать внутренние переменные.

Переменные применяются для хранения промежуточных результатов.

---

### Parameters

Workflow поддерживает входные параметры.

Backend передаёт Workflow только те данные, которые необходимы для выполнения конкретной задачи.

---

### Transform Node

Transform Node — специализированный Node для преобразования данных внутри Workflow (изменение формы данных, вычисление производных значений, фильтрация, объединение полей).

Transform Node подчиняется принципу изоляции:

- Transform Node имеет доступ **только** к своему входу (`input`) — данным, явно переданным на его входные порты.
- Transform Node **не имеет** доступа за пределы своего контекста: к Execution Context других Node, к глобальным переменным Workflow (кроме явно переданных на вход), к файловой системе, к сети, к секретам, к системному времени, к генератору случайных чисел и к любым внешним ресурсам.
- Transform Node не может выполнять побочные эффекты (изменение состояния платформы). Любое изменение данных платформы выполняется исключительно через Backend API (см. § 13.5).

Требование к реализации: Transform Node поддерживает два режима:

- `expression` — ограниченный декларативный вычислитель выражений (safe expression evaluator) над разрешённым списком (whitelist) операций и функций. Это базовый и предпочтительный режим для типовых преобразований.
- `code` — произвольный JavaScript-код. На этапе MVP этот режим допустим, потому что код Transform-схем создаёт и публикует только роль уровня платформы `Platform Operator` (см. §9.3), а не администраторы отдельных организаций и не внешние пользователи. Перед передачей режима `code` администраторам организаций должны быть введены дополнительные меры безопасности: модерация/ревью кода, журналирование изменений, изолированное исполнение с лимитами ресурсов и возможность централизованного отключения небезопасной версии схемы.

Оба режима сохраняют принцип изоляции Transform Node: код/выражение видит только `input`, не получает доступ к секретам, сети, файловой системе, системному времени, генератору случайных чисел и Execution Context других Node. Режим `code` не должен использоваться для побочных эффектов; изменения состояния платформы по-прежнему выполняются только через Backend API (см. §13.5).

Разрешённый набор режима `expression` включает как минимум:

- доступ к полям входного объекта (`input.field`);
- арифметические и логические операции;
- сравнения и условные выражения;
- операции над строками, числами, датами из разрешённого списка;
- операции над массивами и объектами (`map`, `filter`, `join` и подобные) из разрешённого списка.

Любая операция вне разрешённого списка режима `expression` должна отвергаться на этапе валидации Workflow (до сохранения схемы), а не во время исполнения. Для режима `code` валидация схемы проверяет форму конфигурации и права публикации, а безопасность самого кода обеспечивается отдельным контуром исполнения и модерации.

Ограничения ресурсов: вычисление Transform Node должно быть ограничено по времени, памяти и размеру результата, чтобы исключить неограниченное потребление ресурсов (например, чрезмерно большие вычисления или бесконечные циклы). Для режима `expression` это обеспечивается грамматикой без неограниченных циклов и лимитами AST/результата; для режима `code` — песочницей/изолированным исполнителем и лимитами ресурсов.

---

## 13.5 Интеграция с Backend

FBP Engine не изменяет данные платформы самостоятельно.

Если Workflow необходимо изменить данные платформы, выполняется следующая последовательность:

1. Workflow формирует запрос.
2. Backend API получает запрос.
3. Backend выполняет проверку полномочий.
4. Backend применяет бизнес-логику.
5. Backend изменяет данные.
6. Backend возвращает результат Workflow.

Таким образом FBP Engine не обладает административными полномочиями.

---

## 13.6 Использование AI

Workflow может использовать одну или несколько языковых моделей.

Количество моделей не ограничивается архитектурой.

FBP Engine определяет:

- последовательность обращения к моделям;
- передачу контекста;
- обработку результатов;
- объединение ответов нескольких моделей;
- дополнительные проверки.

---

## 13.7 Использование Knowledge Base

Workflow может выполнять поиск информации в базе знаний.

Поиск осуществляется посредством Backend API.

FBP Engine не взаимодействует напрямую с PostgreSQL.

Полученные документы становятся частью Execution Context.

---

## 13.8 Использование внешних сервисов

Workflow может обращаться к внешним сервисам посредством специализированных Connector.

Каждый Connector имеет собственный адаптер безопасности.

Использование внешних сервисов контролируется Backend.

---

## 13.9 Ошибки исполнения

При возникновении ошибки Workflow обязан:

- завершить выполнение;
- вернуть информацию Backend;
- сохранить диагностическую информацию;
- зарегистрировать событие аудита.

Ошибки Workflow не должны приводить к отказу Communication Core.

---

## 13.10 Версионирование Workflow

Каждая схема Workflow должна иметь собственную версию.

Новая версия Workflow не должна изменять поведение уже выполняющихся процессов.

Уже запущенный экземпляр Workflow (Workflow Instance) выполняется до завершения на той версии схемы, с которой он был запущен (version pinning). Публикация новой версии схемы влияет только на экземпляры, запущенные после публикации.

Для этого при запуске экземпляра Workflow фиксируется идентификатор конкретной версии схемы, а исполнитель (executor) загружает схему по этому идентификатору. Версии схем являются неизменяемыми (immutable): изменение схемы порождает новую версию, а не перезаписывает существующую.

Переключение «версии по умолчанию» для новых запусков осуществляется посредством конфигурации.

Данная модель совместима с горизонтальным масштабированием исполнителей: поскольку исполнитель не хранит состояние между шагами (stateless executor, см. § 25.3), а состояние экземпляра хранится вне исполнителя, любой экземпляр Workflow может обрабатываться любым узлом-исполнителем при условии загрузки зафиксированной версии схемы.

---

## 13.11 Импорт и экспорт

После MVP FBP Engine должен поддерживать:

- импорт Workflow;
- экспорт Workflow;
- импорт базы знаний;
- экспорт базы знаний.

Это позволит переносить настройки между организациями и окружениями.

---

## 13.12 Архитектурные ограничения

FBP Engine запрещается:

- выполнять SQL-запросы;
- изменять базу данных напрямую;
- обращаться к внутренним сервисам в обход Backend API;
- изменять полномочия пользователей;
- получать доступ к секретам платформы.

Все действия должны выполняться исключительно посредством документированных Backend API.

Отдельно ограничивается исполнение пользовательской логики внутри Node. Узлы, кроме Transform Node, не имеют права исполнять произвольный код с доступом к среде выполнения. Transform Node может исполнять произвольный JavaScript только в режиме `code`, описанном в §13.4: на этапе MVP такой код создаёт и публикует `Platform Operator`, а перед расширением доступа на администраторов организаций требуется модерация/ревью кода и изолированное исполнение с лимитами ресурсов. Данное ограничение является следствием требования: логика преобразования данных имеет доступ только к `input` и не имеет доступа за пределы своего контекста.

---

## 13.13 Адаптация проекта fbp-engine

FBP Engine платформы строится на основе существующего проекта **fbp-engine** (см. § 4.16), который рассматривается как пример реализации (reference implementation) движка исполнения графов. Проект берётся за основу и дорабатывается (форк с последующей переработкой) под требования Communication Platform; заимствуется архитектурный подход (граф из Node и Connection, Execution Context, пошаговое исполнение), а не прикладной набор узлов.

При адаптации проекта выполняются следующие изменения.

1. **Замена прикладного набора узлов.** Прикладные узлы исходного проекта (ориентированные на его исходную предметную область) заменяются на нейтральный набор узлов коммуникационной платформы: вызов Backend API, вызов LLM, поиск в Knowledge Base, ветвление, преобразование данных (Transform Node), ожидание события. Ядро исполнения остаётся предметно-нейтральным и не содержит логики конкретной предметной области.

2. **Узел вызова Backend API.** Вводится штатный узел вызова Backend API как единственный санкционированный способ изменения данных платформы (см. § 13.5). Любые изменения состояния выполняются только через этот узел.

3. **Удаление прямого доступа к данным.** Из адаптированного движка удаляется любой прямой доступ к базе данных и к внутренним сервисам в обход Backend API (см. § 13.12 и § 22.3). Движок не работает с базой данных напрямую.

4. **Мультиарендность (multitenancy).** Исполнение Workflow привязывается к контексту Tenant/Organization: каждый экземпляр Workflow исполняется в контексте конкретной организации, а все обращения к Backend API выполняются от имени этого контекста с соблюдением изоляции данных между организациями (см. § 22.6). Движок не должен допускать доступ к данным одной организации из Workflow другой организации.

5. **Безопасное преобразование данных.** Узел преобразования данных реализуется как изолированный Transform Node с доступом только к входу. Для типовых преобразований используется ограниченный вычислитель выражений; для совместимости с механикой первоисточника допускается режим `code` с произвольным JavaScript, который в MVP публикуется только `Platform Operator` и требует дополнительных мер модерации/изоляции перед передачей администраторам организаций (см. §13.4).

6. **Масштабирование и версионирование.** Исполнитель приводится к модели без хранения состояния между шагами (stateless executor) для горизонтального масштабирования (см. § 25.3), а исполнение экземпляров Workflow фиксируется на версии схемы (version pinning, см. § 13.10).

Исходная лицензия проекта fbp-engine и совместимость лицензий должны быть проверены до включения кода в состав платформы.

---

# 14. Broadcast Platform

## 14.1 Назначение

Broadcast Platform предназначена для организации массовых коммуникаций между организацией и её клиентами.

Broadcast является прикладной подсистемой платформы и использует Communication Core для доставки сообщений.

Broadcast Platform не взаимодействует напрямую с внешними каналами связи.

---

## 14.2 Основные возможности

Broadcast Platform должна обеспечивать:

- создание кампаний;
- выбор получателей;
- подготовку сообщений;
- персонализацию;
- планирование времени отправки;
- запуск кампаний;
- контроль выполнения;
- получение статистики.

---

## 14.3 Архитектурный принцип

Broadcast Platform не содержит собственной системы доставки сообщений.

После формирования сообщения оно передаётся Communication Core.

Дальнейшая обработка полностью совпадает с обработкой обычной пользовательской переписки.

Это обеспечивает единый механизм доставки сообщений независимо от источника их возникновения.

---

## 14.4 Получатели

Получатели Broadcast определяются фильтрами.

Минимально должны поддерживаться фильтры по:

- организации;
- тегам;
- каналам связи;
- пользовательским сегментам;
- произвольным условиям.

Архитектура должна предусматривать возможность добавления новых критериев отбора.

---

## 14.5 Персонализация

Перед отправкой сообщение может быть персонализировано.

Персонализация выполняется Backend до передачи сообщения Communication Core.

Персонализация может использовать данные клиента и организации.

---

## 14.6 Ограничение скорости

Broadcast Platform должна поддерживать управление скоростью отправки сообщений.

Параметры ограничения определяются:

- возможностями конкретного канала;
- политиками организации;
- инфраструктурными ограничениями.

---

## 14.7 Планирование

Broadcast может запускаться:

- немедленно;
- по расписанию;
- по событию;
- посредством Workflow.

---

## 14.8 Контроль выполнения

Для каждой кампании должна отображаться информация о состоянии выполнения.

Минимально фиксируются:

- количество подготовленных сообщений;
- количество отправленных сообщений;
- количество доставленных сообщений;
- количество ошибок;
- время выполнения.

---

## 14.9 Повторная отправка

При временных ошибках доставки должна поддерживаться повторная отправка сообщений.

Количество попыток определяется настройками платформы.

---

## 14.10 Независимость каналов

Broadcast Platform не зависит от Telegram, WhatsApp, Email, SMS и других платформ.

Все особенности конкретных каналов полностью инкапсулируются внутри Integration Platform.

Broadcast работает исключительно с внутренней моделью сообщений.

---

# 15. Notification Platform

## 15.1 Назначение

Notification Platform предназначена для информирования пользователей платформы о внутренних событиях.

Notification не используется для общения организации с её клиентами.

---

## 15.2 Источники уведомлений

Уведомления могут формироваться:

- Backend;
- Communication Core;
- Broadcast Platform;
- AI Platform;
- Workflow;
- системой безопасности;
- административными сервисами.

---

## 15.3 Получатели уведомлений

Получателями являются исключительно пользователи платформы.

Минимально:

- Administrator;
- Manager.

Конечные клиенты уведомления Notification Platform не получают.

---

## 15.4 Каналы доставки уведомлений

Архитектура должна поддерживать доставку уведомлений посредством:

- Web Interface;
- Telegram Console;
- электронной почты;
- Push-уведомлений.

Перечень поддерживаемых каналов может расширяться.

---

## 15.5 Категории уведомлений

Минимально поддерживаются:

- информационные;
- предупреждения;
- ошибки;
- критические события;
- административные сообщения.

---

## 15.6 Управление уведомлениями

Пользователь должен иметь возможность управлять настройками получения уведомлений.

Настройки определяются индивидуально для каждого пользователя.

---

## 15.7 Архитектурный принцип

Notification Platform не содержит бизнес-логики коммуникации.

Она предназначена исключительно для информирования пользователей платформы о происходящих событиях.

# 16. SaaS Administration

## 16.1 Назначение

SaaS Administration является основным административным интерфейсом платформы.

Через SaaS Administration администраторы организаций выполняют настройку платформы, управление пользователями, каналами связи, AI, автоматизацией и другими компонентами системы.

SaaS Administration является единственным интерфейсом управления конфигурацией организации.

---

## 16.2 Основные функции

Административная панель должна обеспечивать:

- управление организацией;
- управление пользователями;
- управление ролями;
- подключение каналов связи;
- настройку Integration Platform;
- настройку AI Assistant;
- настройку AI Onboarding;
- управление Knowledge Base;
- управление Workflow;
- управление Broadcast;
- просмотр журналов аудита;
- просмотр системных журналов;
- настройку Notification;
- управление параметрами безопасности.

---

## 16.3 Управление организацией

Administrator может изменять:

- название организации;
- описание;
- часовой пояс;
- язык интерфейса;
- параметры AI;
- ограничения использования платформы;
- настройки каналов связи.

Все изменения выполняются через Backend API.

---

## 16.4 Управление пользователями

Администратор может:

- создавать пользователей;
- блокировать пользователей;
- изменять роли;
- отзывать активные сессии;
- изменять параметры доступа.

Удаление пользователей должно выполняться в соответствии с политикой хранения данных платформы.

---

## 16.5 Управление каналами связи

Каждый подключённый канал отображается как отдельная конфигурационная сущность.

Для каждого канала должны отображаться:

- состояние подключения;
- дата последней проверки;
- используемые возможности (Capabilities);
- журнал ошибок;
- параметры подключения.

Подключение нового канала не требует изменения пользовательского интерфейса.

---

## 16.6 Управление Knowledge Base

Administrator должен иметь возможность:

- создавать текстовый документ, задавая два поля: название и контент (инструкция для ассистента);
- редактировать название и контент документа;
- удалять документы;
- просматривать список документов.

Загрузка файлов и ручная переиндексация не предусмотрены: эмбеддинг документа вычисляется автоматически при каждом сохранении (создании или изменении контента).

> **Ревизия (2026-07-14, редизайн Knowledge Base).** Ранее раздел описывал загрузку файлов и отдельную индексацию со статусами. По решению владельца продукта Knowledge Base переведён на модель текстовых инструкций (два поля) с вычислением эмбеддинга при сохранении — по образцу проекта `rumaster/fbp-engine` («Экспертиза»). Эмбеддинг документа вычисляет Backend в момент сохранения (см. §22.7). Требование к согласованности спецификации и кода зафиксировано в `CLAUDE.md`.

Knowledge Base используется исключительно AI Platform.

---

## 16.7 Управление Workflow

Все Workflow отображаются в отдельном разделе административной панели.

Administrator может:

- включать Workflow;
- отключать Workflow;
- выбирать активную версию;
- создавать и редактировать Workflow посредством визуального редактора схем;
- сохранять новую версию Workflow (см. §13.10);
- просматривать историю исполнения;
- просматривать диагностическую информацию.

**Редактирование Workflow входит в объём MVP.** Программируемая автоматизация является ключевой ценностью платформы (§3.4, §5.5), поэтому в MVP администратору предоставляется визуальный редактор схем, входящий в состав адаптированного FBP Engine (§13.1, §13.13).

Редактирование в MVP ограничивается следующими рамками для сохранения безопасности и предсказуемости:

- доступен только безопасный набор узлов; произвольный код допускается только в Transform Node в режиме `code`, публикуемом `Platform Operator` на этапе MVP (см. §13.4, §13.12, §13.13);
- любые изменения данных выполняются исключительно через узел вызова Backend API (§13.5) с проверкой полномочий текущего пользователя;
- изменения сохраняются как новая версия Workflow и не влияют на уже выполняющиеся экземпляры (§13.10);
- все действия редактирования фиксируются в журнале аудита (§23.8).

Расширенные возможности (импорт/экспорт схем, совместное редактирование, библиотека пользовательских узлов) выносятся за пределы MVP (§13.11) и предусматриваются архитектурно.

---

## 16.8 AI Onboarding

Администратор может использовать AI Onboarding для настройки платформы.

AI Onboarding работает в виде диалогового помощника.

После получения подтверждения пользователя AI формирует структурированный запрос к Backend API.

Backend выполняет проверку полномочий и применяет изменения.

---

## 16.9 Архитектурный принцип

SaaS Administration не содержит бизнес-логики.

Все операции выполняются посредством Backend API.

Frontend отвечает исключительно за отображение данных и взаимодействие с пользователем.

---

# 17. Manager Workspace

## 17.1 Назначение

Manager Workspace является основным рабочим местом сотрудников организации.

Все ежедневные операции по взаимодействию с клиентами выполняются через данный интерфейс.

Manager Workspace предоставляет единый интерфейс независимо от канала, используемого клиентом.

---

## 17.2 Основные функции

Manager Workspace должен обеспечивать:

- просмотр очереди обращений;
- поиск клиентов;
- просмотр истории коммуникаций;
- ведение переписки;
- просмотр карточки клиента;
- работу с вложениями;
- использование AI Assistant;
- получение уведомлений;
- запуск Broadcast (при наличии полномочий);
- просмотр истории действий.

---

## 17.3 Единая история коммуникаций

Для каждого клиента отображается единая Conversation.

История сообщений объединяет все поддерживаемые каналы связи.

Менеджер не должен переключаться между различными интерфейсами в зависимости от канала клиента.

---

## 17.4 Карточка клиента

Карточка клиента отображает:

- основные сведения;
- список Communication Endpoint;
- историю коммуникаций;
- активные обращения;
- пользовательские заметки;
- теги;
- историю изменений.

Карточка клиента является логическим представлением данных Backend.

---

## 17.5 Работа с сообщениями

Менеджер может:

- отправлять сообщения;
- получать сообщения;
- просматривать вложения;
- отвечать клиенту;
- пересылать сообщения;
- использовать шаблоны ответов.

После отправки сообщение передаётся Communication Core.

---

## 17.6 Использование AI Assistant

AI Assistant предоставляет менеджеру:

- рекомендации по ответу;
- краткое содержание длинной переписки;
- поиск информации в базе знаний;
- перевод сообщений;
- исправление текста;
- генерацию вариантов ответа.

Менеджер самостоятельно принимает решение об использовании предложений AI.

---

## 17.7 Работа в реальном времени

Manager Workspace использует WebSocket.

Без обновления страницы должны отображаться:

- новые сообщения;
- изменение статусов;
- уведомления;
- изменение очереди обращений.

---

## 17.8 Архитектурный принцип

Manager Workspace является эталонным интерфейсом работы менеджеров.

Все остальные пользовательские интерфейсы должны использовать те же Backend API и ту же бизнес-логику.

---

# 18. Web Chat

## 18.1 Назначение

Web Chat является встроенным клиентским интерфейсом Communication Platform.

Он предназначен для общения конечных пользователей с организацией через веб-браузер.

---

## 18.2 Архитектурная роль

Web Chat рассматривается как один из Communication Endpoint.

С точки зрения Communication Core Web Chat не отличается от Telegram, WhatsApp, MAX или любого другого канала связи.

---

## 18.3 Reference Client

Web Chat является эталонной реализацией клиентского интерфейса платформы.

Все новые возможности платформы в первую очередь должны поддерживаться Web Chat.

Другие клиентские приложения могут реализовывать только поддерживаемое ими подмножество функций.

---

## 18.4 Основные функции

Web Chat должен обеспечивать:

- подключение клиента;
- отправку сообщений;
- получение сообщений;
- работу с вложениями;
- отображение истории переписки;
- отображение статуса соединения;
- отображение статуса доставки сообщений;
- работу в реальном времени.

---

## 18.5 Работа с AI

Если организацией разрешено использование AI Assistant, Web Chat должен обеспечивать возможность общения клиента с AI.

AI взаимодействует с клиентом посредством стандартного механизма Communication Core.

Специального режима общения с AI не существует.

---

## 18.6 Авторизация клиента

Архитектура должна поддерживать как анонимный режим работы, так и идентифицированных пользователей.

Конкретная модель авторизации определяется настройками организации.

Для анонимных сессий действует поведение по умолчанию из §8.13: ведётся отдельная Conversation на сессию до момента идентификации. Присоединение анонимной сессии к существующему Client возможно только после подтверждённой идентификации (авторизация, верификация телефона/почты или явная привязка), что исключает ошибочное слияние анонимного клиента с чужой историей.

---

## 18.7 Работа через Edge Cluster

При размещении клиента в Российской Федерации Web Chat подключается к Edge Cluster.

Edge Cluster обеспечивает обслуживание соединения и передачу сообщений в Application Cluster посредством VPN Tunnel Service.

Для клиента данный процесс полностью прозрачен.

---

## 18.8 Архитектурный принцип

Web Chat не содержит бизнес-логики.

Вся обработка сообщений выполняется Backend и Communication Core.

Web Chat отвечает исключительно за пользовательский интерфейс и взаимодействие с Backend API.

# 19. Mobile API

## 19.1 Назначение

Mobile API предназначен для взаимодействия мобильных приложений с Backend платформы.

Mobile API использует те же бизнес-процессы и модели данных, что и Web-интерфейсы.

Создание отдельной серверной логики для мобильных приложений не допускается.

---

## 19.2 Архитектурный принцип

Mobile API является специализированным представлением Backend API.

Вся бизнес-логика остаётся общей для:

- Web Chat;
- Manager Workspace;
- SaaS Administration;
- Mobile Application.

Это обеспечивает единое поведение платформы независимо от используемого клиентского приложения.

---

## 19.3 Основные функции

Mobile API предоставляет:

- аутентификацию;
- получение списка Conversation;
- получение истории сообщений;
- отправку сообщений;
- работу с вложениями;
- получение уведомлений;
- получение статусов сообщений;
- работу с AI Assistant;
- синхронизацию данных.

---

## 19.4 Работа в реальном времени

Mobile API должен поддерживать получение событий посредством WebSocket.

При невозможности использования постоянного соединения допускается использование Push Notification совместно с последующей синхронизацией данных.

---

## 19.5 Работа через Edge Cluster

При использовании инфраструктуры Российской Федерации мобильное приложение подключается к Edge Cluster.

Все дальнейшее взаимодействие осуществляется через VPN Tunnel Service и Communication Core.

---

## 19.6 Версионирование

Mobile API должен иметь независимое версионирование.

Изменения API не должны нарушать работу ранее опубликованных мобильных приложений.

---

# 20. Telegram Console

## 20.1 Назначение

Telegram Console является дополнительным пользовательским интерфейсом платформы.

Основная задача Telegram Console — предоставить менеджеру возможность оперативной работы без открытия Web-интерфейса.

Telegram Console не заменяет Manager Workspace.

Она предназначена для выполнения наиболее востребованных операций.

---

## 20.2 Основные сценарии использования

Telegram Console используется для:

- получения уведомлений;
- просмотра новых обращений;
- быстрого ответа клиенту;
- просмотра информации о клиенте;
- получения рекомендаций AI;
- получения ссылок на соответствующие разделы Manager Workspace.

---

## 20.3 Получение уведомлений

При возникновении важных событий Backend может отправлять уведомления менеджеру через Telegram Bot.

Примеры уведомлений:

- новое обращение клиента;
- новое сообщение;
- критическое уведомление;
- ошибка Broadcast;
- завершение Workflow;
- административные сообщения.

Перечень уведомлений определяется настройками пользователя.

---

## 20.4 Ответ клиенту

Менеджер может ответить клиенту непосредственно из Telegram Console.

Последовательность обработки ответа полностью совпадает с обычной перепиской:

1. сообщение получает Backend;
2. сообщение проходит проверки безопасности;
3. сообщение поступает в Communication Core;
4. Communication Core передаёт сообщение соответствующему Adapter;
5. сообщение доставляется клиенту.

Telegram Console не взаимодействует с каналами связи напрямую.

---

## 20.5 Просмотр информации о клиенте

Telegram Console должна обеспечивать быстрый просмотр:

- имени клиента;
- используемого канала;
- краткой истории переписки;
- количества непрочитанных сообщений;
- текущего статуса обращения.

Полная карточка клиента доступна только в Manager Workspace.

---

## 20.6 Использование AI

Менеджер может запросить рекомендации AI непосредственно из Telegram Console.

AI может предоставить:

- краткое содержание диалога;
- рекомендуемый ответ;
- поиск информации в базе знаний;
- перевод сообщения.

Применение рекомендаций AI всегда выполняется менеджером вручную.

---

## 20.7 Ограничения Telegram Console

Telegram Console не предназначена для выполнения административных операций.

Через Telegram Console запрещается:

- управление пользователями;
- изменение настроек организации;
- изменение Workflow;
- изменение параметров безопасности;
- управление каналами связи.

Все административные операции выполняются исключительно через SaaS Administration.

---

## 20.8 Архитектурный принцип

Telegram Console является альтернативным пользовательским интерфейсом.

Она использует те же Backend API, что и остальные приложения платформы.

Никакой отдельной бизнес-логики для Telegram Console не существует.

---

# 21. Frontend

## 21.1 Общие положения

Все пользовательские интерфейсы платформы реализуются с использованием **React** и **Vite**.

Frontend отвечает исключительно за представление данных и взаимодействие с пользователем.

Вся бизнес-логика располагается в Backend.

---

## 21.2 Состав Frontend

Frontend платформы включает следующие независимые приложения:

- SaaS Administration;
- Manager Workspace;
- Web Chat.

Архитектура должна предусматривать возможность подключения новых приложений без изменения Backend.

---

## 21.3 Общая компонентная библиотека

Все интерфейсы используют единую библиотеку компонентов.

Переиспользуемыми должны быть:

- элементы форм;
- таблицы;
- карточки;
- модальные окна;
- элементы навигации;
- средства отображения сообщений;
- уведомления;
- компоненты работы с файлами.

Дублирование компонентов между приложениями не допускается.

---

## 21.4 Архитектура приложений

Каждое приложение должно иметь модульную структуру.

Основные слои:

- Presentation;
- API Client;
- State Management;
- Routing;
- Shared Components.

Бизнес-логика внутри Frontend не размещается.

---

## 21.5 Работа с Backend

Frontend взаимодействует исключительно посредством Backend API.

Запрещается:

- прямой доступ к PostgreSQL;
- выполнение SQL-запросов;
- хранение бизнес-логики;
- принятие решений о правах доступа.

Все проверки выполняются Backend.

---

## 21.6 Работа в реальном времени

Все пользовательские интерфейсы используют WebSocket для получения событий.

При поступлении новых сообщений пользовательский интерфейс обновляется автоматически.

Обновление страницы не требуется.

---

## 21.7 Производительность

Frontend должен обеспечивать:

- ленивую загрузку модулей;
- оптимизацию размеров сборки;
- повторное использование компонентов;
- минимизацию количества сетевых запросов.

---

## 21.8 Адаптивность

Минимально должны поддерживаться:

- Desktop;
- Tablet.

Архитектура должна предусматривать возможность дальнейшей адаптации интерфейсов для мобильных браузеров.

---

## 21.9 Архитектурный принцип

Все Frontend-приложения являются клиентами единой платформы.

Они не содержат собственной бизнес-логики и используют одинаковые Backend API.

Это обеспечивает единое поведение платформы независимо от используемого пользовательского интерфейса.

# 22. Data Platform

## 22.1 Общие положения

Data Platform обеспечивает централизованное хранение данных Communication Platform.

На этапе MVP используется единая база данных PostgreSQL с расширением pgvector.

Архитектура должна предусматривать возможность последующего масштабирования без изменения бизнес-логики платформы.

---

## 22.2 Используемые технологии

В качестве основной СУБД используется:

- PostgreSQL 16;
- pgvector.

Использование других СУБД в рамках MVP не предусматривается.

---

## 22.3 Архитектурный принцип

Backend является единственным компонентом, имеющим доступ к базе данных.

Ни один другой компонент платформы не может обращаться к PostgreSQL напрямую.

В частности запрещён прямой доступ для:

- AI Platform;
- FBP Engine;
- Frontend;
- Integration Platform;
- Telegram Console;
- внешних сервисов.

Все операции выполняются исключительно через Backend API.

---

## 22.4 Логическая структура данных

Структура данных проектируется вокруг следующих основных доменных сущностей:

### Organization

Организация, использующая платформу.

---

### User

Пользователь административной части платформы.

---

### Role

Роль пользователя.

---

### Client

Конечный пользователь организации.

---

### Communication Endpoint

Конкретный способ связи клиента.

---

### Conversation

История общения клиента с организацией.

---

### Message

Отдельное сообщение.

---

### Attachment

Вложение сообщения.

---

### Broadcast

Массовая рассылка.

---

### Notification

Внутреннее уведомление.

---

### Workflow

Исполняемая схема FBP Engine.

---

### Knowledge Document

Документ базы знаний.

---

### Audit Event

Событие аудита.

---

### Configuration

Конфигурационные параметры организации.

---

## 22.5 Проектирование структуры БД

Настоящее техническое задание определяет логическую модель данных.

Физическая структура базы данных (таблицы, поля, индексы, ограничения, типы данных и способы оптимизации) разрабатывается отдельно в рамках специализированного проекта по проектированию базы данных.

Исполнитель обязан обеспечить полное соответствие физической модели логической модели, описанной настоящим документом.

---

## 22.6 Изоляция данных организаций

Все данные платформы логически принадлежат конкретной организации.

Любая операция чтения или изменения данных должна учитывать принадлежность объекта соответствующему Tenant.

Получение данных другой организации не допускается.

Контроль изоляции выполняется исключительно Backend.

---

## 22.7 Работа с pgvector

Хранилище векторов pgvector используется исключительно AI-контуром (AI Assistant / AI Platform). Communication Core не использует pgvector.

Векторизации подлежат:

- документы базы знаний;
- дополнительные текстовые материалы организации.

Поиск выполняется посредством поиска семантически близких документов.

**Вычисление эмбеддингов.** Эмбеддинг документа базы знаний вычисляется Backend в момент сохранения документа (создание/изменение) и записывается в pgvector (`knowledge_chunks`). Эмбеддинг поискового запроса вычисляется SVC-AI (ai-platform) в момент поиска. Обе стороны обязаны использовать одну и ту же модель эмбеддингов и одинаковую размерность (1536), иначе расстояния становятся бессмысленными; конфигурация модели (`EMBEDDING_*` / `LLM_EMBEDDING_MODEL_NAME`) должна совпадать у Backend и ai-platform.

> **Ревизия (2026-07-14).** Вычисление эмбеддинга документов Backend'ом на сохранении — сознательное отклонение от исходной формулировки «эмбеддинги считает только SVC-AI»: выбран более простой путь по образцу `rumaster/fbp-engine`. Риск рассинхронизации моделей закрыт требованием общей конфигурации выше. См. `CLAUDE.md` о приоритете фактического решения над спецификацией.

---

## 22.8 Полнотекстовый поиск

Архитектура должна предусматривать возможность поиска по:

- сообщениям;
- клиентам;
- документам;
- организациям;
- пользователям.

Способ реализации определяется проектом базы данных.

---

## 22.9 Журнал аудита

Изменения критически важных объектов должны сопровождаться записью события аудита.

Минимально фиксируются:

- пользователь;
- организация;
- время;
- операция;
- изменяемый объект;
- результат выполнения.

---

## 22.10 Историчность данных

Архитектура должна предусматривать возможность хранения истории изменений конфигурации.

Удаление данных не должно нарушать целостность истории коммуникаций.

---

## 22.11 Удаление персональных данных и обезличивание

Требование удаления персональных данных клиента (в том числе по обращению субъекта данных или по истечении срока хранения) вступает в противоречие с требованием неизменности журнала аудита (см. § 22.9, § 23.8) и целостности истории коммуникаций (см. § 22.10). Настоящий раздел определяет способ разрешения этого противоречия.

Разделяются два класса данных.

1. **Идентифицирующие персональные данные** — данные, позволяющие прямо или косвенно определить субъекта (имя, номер телефона, адрес электронной почты, идентификатор во внешнем мессенджере, содержимое сообщений и т. п.).
2. **Обезличенные и служебные данные** — данные, не позволяющие определить субъекта без привлечения дополнительной информации: суррогатные идентификаторы объектов, отметки времени, статусы, агрегаты, факт наступления события в журнале аудита.

Стратегия удаления — **обезличивание вместо физического удаления связанных исторических записей** (депонирование по принципу «обезличивание, а не разрушение целостности»).

- При исполнении запроса на удаление идентифицирующие персональные данные субъекта удаляются или необратимо обезличиваются (стираются либо заменяются на необратимый суррогат), а структура истории коммуникаций и записи журнала аудита сохраняются в обезличенном виде.
- Идентификаторы, используемые как ссылки в истории и аудите, являются суррогатными (не выводимыми из персональных данных), поэтому сохранение этих идентификаторов после обезличивания не раскрывает субъекта.
- Обезличивание должно быть необратимым: не должна сохраняться таблица соответствия, позволяющая восстановить исходные персональные данные (это отличает обезличивание от псевдонимизации; см. § 23.9).
- Сам факт и время операции обезличивания фиксируются в журнале аудита без раскрытия удалённых персональных данных.

Данная модель позволяет одновременно исполнить право на удаление персональных данных и сохранить неизменность и целостность журнала аудита и историческую статистику коммуникаций.

Конкретные сроки хранения и перечень удаляемых полей определяются политикой обработки персональных данных оператора (см. § 23.9).

---

# 23. Безопасность

## 23.1 Общие положения

Безопасность платформы должна соответствовать современным требованиям к корпоративным SaaS-системам.

Все архитектурные решения должны приниматься с учётом принципа Security by Design.

---

## 23.2 Основные принципы

При проектировании платформы используются следующие принципы:

- минимально необходимые полномочия;
- разделение ответственности;
- защита данных при передаче;
- защита данных при хранении;
- аудит действий;
- обязательная аутентификация пользователей;
- серверная проверка полномочий.

---

## 23.3 Передача данных

Все соединения должны использовать TLS.

Передача данных между Edge Cluster и Application Cluster осуществляется через защищённый VPN Tunnel Service с взаимной аутентификацией.

---

## 23.4 Управление доступом

Доступ определяется совокупностью:

- роли пользователя;
- принадлежности организации;
- типа операции;
- состояния объекта.

Frontend не участвует в принятии решений о предоставлении доступа.

---

## 23.5 Защита API

Backend обязан реализовать:

- проверку токенов доступа;
- защиту от повторной отправки запросов;
- ограничение частоты запросов (Rate Limiting);
- защиту от массового перебора;
- серверную валидацию всех входящих данных.

---

## 23.6 Защита AI

AI Platform должна работать в режиме ограниченных полномочий.

AI запрещается:

- выполнять произвольный код;
- выполнять SQL-запросы;
- получать доступ к внутренним сервисам;
- изменять данные напрямую;
- использовать секреты платформы.

Все действия AI проходят через Backend API.

---

## 23.7 Управление секретами

Пароли, ключи API, токены и иные секреты не должны храниться в исходном коде приложения.

Использование централизованного механизма управления секретами является обязательным.

---

## 23.8 Аудит безопасности

Должны регистрироваться:

- входы пользователей;
- ошибки аутентификации;
- административные действия;
- изменение ролей;
- изменение конфигурации;
- обращения AI к Backend API;
- выполнение Workflow.

Журнал аудита должен быть защищён от изменения пользователями.

---

## 23.9 Соответствие законодательству

Архитектура должна обеспечивать возможность соблюдения требований законодательства Российской Федерации в части обработки персональных данных.

При использовании зарубежного кластера взаимодействие между региональными площадками осуществляется посредством VPN Tunnel Service.

**Локализация персональных данных (152-ФЗ).** Платформа реализует модель RF-first: первичный приём и запись персональных данных граждан РФ выполняются в первичной базе данных на территории РФ, а передача в зарубежный Application Cluster осуществляется только после первичной записи и как трансграничная передача с уведомлением Роскомнадзора. Требования и последовательность действий определены в § 7.14; обоснование и источники приведены в Приложении В.

**Обезличивание и удаление.** За пределы РФ и в аналитические подсистемы предпочтительно передаются необратимо обезличенные данные (только они выходят из-под 152-ФЗ; псевдонимизация не выводит данные из-под закона). Исполнение права на удаление персональных данных при сохранении целостности журнала аудита и истории коммуникаций выполняется методом необратимого обезличивания (см. § 22.11).

**Уведомления Роскомнадзора.** Оператор обеспечивает подачу уведомления об обработке ПДн (с указанием места нахождения БД), уведомления о трансграничной передаче до её начала (ст. 12; обязанность введена ФЗ № 266-ФЗ от 14.07.2022, действует с 01.03.2023) и уведомлений об инцидентах (в течение 24/72 часов).

Конкретная модель обработки персональных данных определяется политикой эксплуатации платформы и требованиями законодательства на момент внедрения и подлежит правовой экспертизе.

---

# 24. Мониторинг и журналирование

## 24.1 Назначение

Платформа должна обеспечивать централизованный сбор информации о состоянии всех компонентов.

Мониторинг является обязательной частью архитектуры.

---

## 24.2 Логирование

Все серверные компоненты используют единый формат журналирования.

Минимально регистрируются:

- время;
- компонент;
- уровень сообщения;
- идентификатор запроса;
- пользователь;
- организация;
- описание события.

---

## 24.3 Метрики

Для каждого компонента должны собираться метрики производительности.

Минимально:

- количество запросов;
- среднее время обработки;
- количество ошибок;
- использование памяти;
- использование процессора;
- количество активных соединений;
- длина очередей.

---

## 24.4 Health Check

Каждый серверный компонент предоставляет endpoint проверки работоспособности.

Health Check используется системой оркестрации и мониторинга.

---

## 24.5 Трассировка запросов

Каждый внешний запрос получает уникальный идентификатор.

Идентификатор сохраняется на протяжении всего жизненного цикла обработки запроса.

Это позволяет отслеживать прохождение запроса через все компоненты платформы.

---

## 24.6 Мониторинг Workflow

Для каждого Workflow отображаются:

- количество запусков;
- успешные выполнения;
- ошибки;
- среднее время исполнения;
- активные экземпляры.

---

## 24.7 Мониторинг AI

Должны отображаться:

- количество запросов к AI;
- используемые модели;
- время ответа;
- ошибки;
- стоимость использования (при наличии внешних LLM).

---

## 24.8 Архитектурный принцип

Средства мониторинга не должны влиять на бизнес-логику платформы.

Их отключение не должно приводить к отказу основных функций Communication Platform.

# 25. Нефункциональные требования

## 25.1 Общие положения

Настоящий раздел определяет требования к качественным характеристикам Communication Platform.

Нефункциональные требования являются обязательными для всех компонентов системы независимо от их назначения.

---

## 25.2 Производительность

Архитектура платформы должна обеспечивать обработку пользовательских операций без заметных задержек.

При нормальной нагрузке время ответа Backend API должно соответствовать следующим требованиям:

| Операция | Целевое время |
|-----------|---------------:|
| Авторизация | ≤ 1 сек |
| Получение списка диалогов | ≤ 1 сек |
| Получение истории сообщений | ≤ 2 сек |
| Отправка сообщения | ≤ 1 сек |
| Создание Broadcast | ≤ 3 сек |
| Ответ AI (без учёта времени LLM) | ≤ 500 мс |
| Получение уведомлений | ≤ 1 сек |

Время ответа внешних LLM в данные показатели не входит.

---

## 25.3 Масштабируемость

Архитектура должна поддерживать горизонтальное масштабирование следующих компонентов:

- Backend API;
- Communication Core;
- Adapter;
- WebSocket Gateway;
- AI Platform;
- FBP Engine.

Добавление новых экземпляров компонентов не должно требовать изменения прикладной логики.

Горизонтальное масштабирование FBP Engine основано на модели исполнителя без хранения состояния между шагами (stateless executor): исполнитель не хранит состояние экземпляра Workflow в собственной памяти между шагами, состояние экземпляра хранится вне исполнителя. Это позволяет обрабатывать любой экземпляр Workflow любым узлом-исполнителем и совместимо с фиксацией версии схемы для запущенных экземпляров (version pinning, см. § 13.10).

---

## 25.4 Отказоустойчивость

Кратковременная недоступность одного из компонентов не должна приводить к полной остановке платформы.

Особенно это касается:

- AI Platform;
- FBP Engine;
- отдельных Adapter;
- VPN Tunnel Service.

Communication Core должен продолжать обработку сообщений даже при деградации вспомогательных сервисов.

---

## 25.5 Доступность

Архитектура должна предусматривать возможность достижения высокой доступности посредством стандартных средств оркестрации контейнеров.

Конкретные SLA определяются эксплуатационной документацией.

---

## 25.6 Сопровождаемость

Кодовая база должна быть организована таким образом, чтобы:

- каждый модуль имел ограниченную область ответственности;
- изменения были локализованы;
- тестирование выполнялось независимо;
- документация автоматически обновлялась при изменении API.

---

## 25.7 Расширяемость

Добавление нового канала связи, нового Workflow, новой AI-модели или нового пользовательского интерфейса не должно требовать изменения архитектуры платформы.

---

## 25.8 Переносимость

Платформа должна поддерживать развёртывание в любом Kubernetes-совместимом окружении.

Использование инфраструктурно-зависимых решений должно быть минимизировано.

---

## 25.9 Контейнеризация

Все серверные компоненты должны поставляться в виде OCI/Docker-совместимых контейнеров.

Каждый компонент должен иметь независимый Dockerfile.

---

## 25.10 Конфигурирование

Все параметры окружения должны храниться во внешней конфигурации.

Изменение конфигурации не должно требовать изменения исходного кода.

---

## 25.11 Ориентиры по нагрузке и восстановлению

Настоящий раздел задаёт целевые ориентиры по нагрузочной способности и восстановлению для проектирования и приёмки. Значения приведены как порядковые ориентиры (targets, а не гарантированные SLA) и подлежат уточнению по результатам нагрузочного тестирования и согласования эксплуатационной политики. На стадии MVP профиль нагрузки и точные значения RPO/RTO не являются определяющими и приведены для задания порядка величин и проверки архитектурных решений на масштабируемость.

### Профиль нагрузки (ориентиры)

Проектирование должно исходить из возможности достижения (за счёт горизонтального масштабирования, см. § 25.3) следующих порядков величин:

- количество организаций (Tenant) на инсталляцию — до сотен;
- количество одновременно активных операторских сессий (Manager Workspace) — до тысяч;
- поток входящих и исходящих сообщений — до сотен сообщений в секунду на инсталляцию с возможностью роста добавлением экземпляров Adapter, Communication Core и WebSocket Gateway;
- объём кампаний Broadcast — до сотен тысяч получателей на кампанию с соблюдением ограничений скорости внешних каналов (см. § 14.6).

Точные значения профиля нагрузки уточняются заказчиком до нагрузочного тестирования. Приведённые ориентиры задают порядок величин, на который рассчитывается архитектура.

### Восстановление после сбоев (ориентиры RPO/RTO)

Целевые ориентиры устойчивости к сбоям задаются раздельно по классам данных и подсистем.

| Класс данных / подсистема | Ориентир RPO (допустимая потеря данных) | Ориентир RTO (время восстановления) |
| --- | --- | --- |
| История коммуникаций и персональные данные (Data Platform, RF-контур) | близко к нулю (синхронная фиксация принятых сообщений) | минуты — единицы часов |
| Конфигурация организаций, Workflow, Knowledge Base | близко к нулю | минуты — единицы часов |
| Журнал аудита | ноль (потеря записей аудита недопустима) | минуты — единицы часов |
| Буфер сообщений Edge Cluster при недоступности канала до Application Cluster | ограничен ёмкостью и TTL буфера (см. § 7.9) | восстановление доставки после восстановления канала |
| Вспомогательные подсистемы (AI Platform, FBP Engine) | допускается потеря незавершённых вычислений (переисполнение Workflow) | автоматическое восстановление средствами оркестрации |

Приём входящего сообщения подтверждается клиенту только после его надёжной фиксации (см. § 8.3, § 11.12), что обеспечивает RPO, близкий к нулю, для принятых сообщений.

Конкретные значения RPO/RTO, стратегия резервного копирования и регламент восстановления определяются эксплуатационной документацией и уточняются по результатам согласования с заказчиком.

---

# 26. Требования к тестированию

## 26.1 Общие положения

Все компоненты платформы должны сопровождаться автоматизированными тестами.

Разработка функциональности без автоматического тестирования не допускается.

---

## 26.2 Виды тестирования

Проект должен содержать следующие категории тестов:

- Unit Tests;
- Integration Tests;
- End-to-End Tests.

---

## 26.3 Unit Tests

Unit Tests проверяют отдельные компоненты бизнес-логики.

Минимально тестируются:

- сервисы Backend;
- обработчики сообщений;
- валидаторы;
- преобразователи данных;
- сервисы авторизации;
- сервисы AI;
- Workflow Adapter.

Unit Tests не используют реальные внешние сервисы.

---

## 26.4 Integration Tests

Integration Tests проверяют взаимодействие компонентов платформы.

Минимально должны проверяться:

- Backend ↔ PostgreSQL;
- Backend ↔ Communication Core;
- Backend ↔ AI Platform;
- Backend ↔ FBP Engine;
- Backend ↔ Integration Platform;
- Backend ↔ WebSocket.

---

## 26.5 End-to-End Tests

End-to-End тесты моделируют реальные сценарии использования платформы.

Основная цель E2E — подтвердить, что пользовательский сценарий полностью выполняется через все уровни системы.

---

## 26.6 Обязательные E2E сценарии

Минимальный набор сценариев включает:

### Авторизация

- вход через Telegram Bot;
- получение одноразового кода;
- создание пользовательской сессии.

---

### Работа менеджера

- открытие Manager Workspace;
- получение списка диалогов;
- просмотр истории сообщений;
- отправка сообщения;
- получение ответа клиента.

---

### Web Chat

- открытие Web Chat;
- создание новой Conversation;
- обмен сообщениями;
- доставка сообщений менеджеру.

---

### Telegram

- получение сообщения из Telegram;
- обработка Adapter;
- регистрация Communication Core;
- отображение сообщения менеджеру;
- отправка ответа;
- доставка ответа клиенту.

---

### Broadcast

- создание кампании;
- выбор получателей;
- запуск;
- доставка сообщений.

---

### AI Assistant

- запрос пользователя;
- поиск информации в Knowledge Base;
- генерация ответа;
- отправка ответа клиенту.

---

### AI Onboarding

- запрос администратора;
- генерация структурированной команды;
- выполнение Backend API;
- изменение конфигурации организации.

---

### Workflow

- запуск Workflow;
- выполнение Node;
- завершение Workflow;
- журналирование.

---

### Notification

- генерация уведомления;
- доставка пользователю;
- отображение в Manager Workspace;
- отображение в Telegram Console.

---

### Edge Cluster

- подключение пользователя РФ;
- передача данных через VPN Tunnel Service;
- успешная обработка сообщения.

---

### Потеря соединения

- временная потеря связи между Edge Cluster и Application Cluster;
- накопление сообщений в буфере Edge Cluster (§7.9);
- восстановление соединения;
- отсутствие потери и дублирования сообщений (дедупликация по сквозному `idempotency_key`, §11.12);
- корректная доставка сообщений в исходном порядке в рамках одного Endpoint/Conversation (§7.10).

---

## 26.7 Покрытие тестами

Все критически важные бизнес-процессы должны иметь автоматические E2E-тесты.

Регрессионное тестирование должно выполняться автоматически при каждом изменении платформы.

---

# 27. Требования к разработке

## 27.1 Backend

Backend разрабатывается на:

- NestJS;
- TypeScript.

Архитектурный стиль синхронного ядра — Modular Monolith (модульный монолит на NestJS). Ядро объединяет подсистемы с сильной транзакционной связностью и разворачивается единым процессом. Вспомогательные подсистемы (AI Platform, FBP Engine, Adapters, WebSocket Gateway) выносятся из процесса ядра как деградируемые сервисы. Подробнее двухуровневая модель исполнения описана в § 11.2.

---

## 27.2 Frontend

Frontend разрабатывается на:

- React;
- Vite;
- TypeScript.

---

## 27.3 База данных

Используется:

- PostgreSQL 16;
- pgvector.

---

## 27.4 Контейнеризация

Все компоненты платформы должны поддерживать запуск в Docker.

---

## 27.5 Кодирование

Для всего проекта используются единые правила:

- ESLint;
- Prettier;
- единый Style Guide;
- единые соглашения по именованию.

---

## 27.6 Документирование

Все публичные API документируются посредством OpenAPI.

Все архитектурные решения сопровождаются диаграммами.

---

# 28. Требования к документации

Исполнитель обязан предоставить:

- архитектурную документацию;
- OpenAPI;
- описание модулей;
- описание Workflow;
- руководство администратора;
- руководство пользователя;
- руководство по развёртыванию;
- руководство по эксплуатации.

Документация должна храниться в формате Markdown и сопровождаться системой контроля версий.

---

# 29. Критерии приёмки

Проект считается соответствующим настоящему техническому заданию при выполнении следующих условий:

1. Реализованы все функциональные требования настоящего документа.
2. Архитектура соответствует описанным принципам.
3. Все пользовательские интерфейсы используют единый Backend API.
4. Все поддерживаемые каналы связи функционируют через Integration Platform.
5. Communication Core является единой точкой обработки сообщений.
6. AI Assistant и AI Onboarding реализованы как независимые подсистемы.
7. FBP Engine интегрирован в платформу и используется для исполнения Workflow.
8. Все автоматические тесты успешно выполняются.
9. Документация полностью соответствует реализованной системе.
10. Платформа успешно проходит приёмочные испытания по утверждённым сценариям.

---

# Приложение А. Итоговая архитектура платформы

## Технологический стек

| Компонент | Технология |
|-----------|------------|
| Backend | NestJS |
| Frontend | React + Vite |
| Язык | TypeScript |
| База данных | PostgreSQL 16 |
| Векторный поиск | pgvector (расширение PostgreSQL) |
| AI Workflow | fbp-engine (адаптированный) |
| Контейнеризация | Docker |
| Оркестрация | Kubernetes |
| API | REST + WebSocket |
| Документация | OpenAPI |

---

## Основные подсистемы

- Edge Cluster (РФ)
- VPN Tunnel Service
- Application Cluster
- Backend API (Modular Monolith)
- Communication Core
- Identity Platform
- Integration Platform
- AI Platform
- FBP Engine
- Broadcast Platform
- Notification Platform
- SaaS Administration
- Manager Workspace
- Web Chat
- Telegram Console
- Mobile API
- PostgreSQL + pgvector

---

# Приложение Б. Размещение зарубежного кластера

| Критерий | Европа (например, Германия, Нидерланды, Финляндия) | СНГ (например, Казахстан, Армения) |
|----------|-----------------------------------------------------|------------------------------------|
| Задержка для пользователей РФ | Средняя | Минимальная |
| Соответствие европейским стандартам защиты данных | Высокое | Зависит от страны |
| Доступность облачной инфраструктуры | Максимальная | Ограниченная |
| Выбор провайдеров | Очень широкий | Ограниченный |
| Интеграция с международными сервисами | Наиболее удобна | Может быть ограничена |
| Стоимость эксплуатации | Выше | Ниже |
| Юридическая устойчивость | Выше для международного SaaS | Может зависеть от локального законодательства |
| Масштабирование | Практически без ограничений | Ограничено рынком инфраструктуры |

**Рекомендуемый вариант:** размещение основного **Application Cluster** в одной из стран Европейского союза с развитой облачной инфраструктурой. Это обеспечивает максимальную совместимость с международными сервисами, высокую доступность оборудования и лучшую перспективу масштабирования глобального SaaS. При необходимости работа с пользователями Российской Федерации осуществляется через **Edge Cluster** и **VPN Tunnel Service**, что соответствует принятой архитектурной модели.

**Учёт требований трансграничной передачи (152-ФЗ).** Выбор юрисдикции Application Cluster должен согласовываться с моделью локализации ПДн (§ 7.14): первичная база данных остаётся в РФ, а передача в зарубежный кластер является трансграничной. Предпочтительны страны, обеспечивающие адекватную защиту ПДн (участники Конвенции 108 и перечень по Приказу Роскомнадзора № 128 от 05.08.2022) — для них передача возможна после подачи уведомления, тогда как для стран вне перечня требуется предварительное решение Роскомнадзора. Данный критерий следует учитывать наряду с приведёнными в таблице.

---

# Приложение В. Открытые вопросы и принятые решения

Настоящее приложение фиксирует ответы заказчика на открытые вопросы, сформулированные в ходе ревизии технического задания (см. `docs/MessengerBridge_TZ_Review.md`), и принятые на их основе решения. Приложение носит справочный характер; нормативные требования изложены в соответствующих разделах документа.

## В.1 Программируемость в MVP

**Вопрос.** Включать ли программируемость (визуальное редактирование Workflow) в объём MVP или отложить на последующие этапы?

**Решение заказчика.** Включить программируемость в MVP.

**Отражено в документе:** § 3.4, § 5.5, § 16.7. Редактирование Workflow входит в MVP; при этом сохраняются ограничения безопасности узлов. Произвольный код допустим только для Transform Node в режиме `code`, который на этапе MVP публикует `Platform Operator`; перед передачей этого режима администраторам организаций требуются модерация/ревью кода и изолированное исполнение с лимитами ресурсов — см. §13.4, §13.12.

## В.2 Профиль нагрузки, RPO и RTO

**Вопрос.** Заданы ли на текущем этапе профиль нагрузки и целевые значения RPO/RTO?

**Решение заказчика.** На текущем этапе не являются определяющими; значения задаются на усмотрение исполнителя как ориентиры.

**Отражено в документе:** § 25.11 — приведены порядковые ориентиры по нагрузке и восстановлению, отмеченные как targets, а не гарантированные SLA, с уточнением по результатам нагрузочного тестирования.

## В.3 Локализация персональных данных (152-ФЗ)

**Вопрос.** Как согласуется размещение первичной обработки данных за рубежом (Application Cluster) с требованиями законодательства РФ о локализации ПДн? Требовалось исследовать нормы и практику и принять решение.

**Решение (по итогам исследования).** Принята модель RF-first: первичная запись ПДн граждан РФ — в первичной базе данных на территории РФ; зарубежный кластер работает с вторичной репликой; передача за рубеж оформляется как трансграничная с уведомлением Роскомнадзора; за пределы РФ предпочтительно передаются необратимо обезличенные данные.

**Отражено в документе:** § 3.6, § 7.13, § 7.14, § 22.11, § 23.9, Приложение Б.

**Основание (нормы и источники).**

- Часть 5 статьи 18 152-ФЗ (локализация при сборе); ужесточение с 01.07.2025 (ФЗ № 23-ФЗ от 28.02.2025): использование зарубежных БД на стадии сбора запрещено. Первичная БД — в РФ, зарубежная копия допустима.
- Статья 12 152-ФЗ (трансграничная передача); уведомление до начала передачи введено ФЗ № 266-ФЗ от 14.07.2022 (действует с 01.03.2023). Перечень стран с адекватной защитой — Приказ Роскомнадзора № 128 от 05.08.2022.
- Разъяснения Роскомнадзора (24.03.2025) и Минцифры (12.05.2025): локализация и трансграничная передача — раздельные кумулятивные обязанности; передача ранее локализованных в РФ данных нормой ч. 5 ст. 18 не ограничивается.
- Необратимо обезличенные (анонимизированные) данные выходят из-под 152-ФЗ; псевдонимизация (обратимая) — не выводит.
- Штрафы за нарушение локализации (КоАП, ст. 13.11 в ред. ФЗ № 420-ФЗ, с 30.05.2025): 1–6 млн ₽ (повторно 6–18 млн ₽), возможна блокировка ресурса.

Ключевые источники:

- ФЗ-152 (ст. 12, 18): https://www.consultant.ru/document/cons_doc_LAW_61801/
- Разъяснения РКН/Минцифры 2025 (RF-first): https://comply.ru/tpost/c43ezsout1-lokalizatsiya-i-transgranichnaya-peredac
- Первичная БД в РФ, буфер/перехватчик: https://b-152.ru/hranenie-personalnyh-dannyh-za-granicej
- Анализ ФЗ № 23-ФЗ (нет офиц. толкования «сбора»): https://b1.ru/insights/law-messenger/localization-of-personal-data-of-russian-citizens-6-march-2025/
- Трансграничная передача, Приказ № 128, сроки: https://data-sec.ru/personal-data/cross-border-countries/
- Портал РКН (трансграничная передача): https://pd.rkn.gov.ru/cross-border-transmission/
- Реестр операторов: https://pd.rkn.gov.ru/operators-registry/
- Штрафы (ФЗ № 420-ФЗ с 30.05.2025): https://www.consultant.ru/legalnews/28492/
- Мессенджеры/обезличивание для аналитики: https://enkod.io/blog/zakon-o-personalnyh-dannyh-chto-nuzhno-znat-crm-marketologu-ob-ispolzovanii-telegram-whatsapp-i-google-analytics/
- Аттестованное по 152-ФЗ облако (пример): https://yandex.cloud/solutions/152-fz

**Оговорка.** Сведения актуальны на дату ревизии документа и не являются юридической консультацией; итоговая модель обработки ПДн подлежит правовой экспертизе на момент внедрения.

## В.4 Использование fbp-engine

**Вопрос.** Как относиться к проекту fbp-engine — как к готовой зависимости или как к примеру реализации для доработки?

**Решение заказчика.** Рассматривать fbp-engine как пример реализации (reference implementation), который берётся за основу и перерабатывается под нужды проекта (форк с доработкой).

**Отражено в документе:** § 4.16, § 13.1, § 13.13 (адаптация: замена набора узлов на нейтральный коммуникационный, узел вызова Backend API, удаление прямого доступа к данным, мультиарендность, безопасный Transform Node, stateless-исполнитель и версионирование).

## В.5 Резервный способ входа администратора

**Вопрос.** Нужен ли резервный (не через Telegram) способ входа администратора, и реализовывать ли его в MVP?

**Решение заказчика.** Предусмотреть архитектурную расширяемость для резервного входа, но не реализовывать его в MVP.

**Отражено в документе:** § 9.4, § 9.6 (резервный вход по email + одноразовый код как точка расширения, вне объёма MVP).

---
