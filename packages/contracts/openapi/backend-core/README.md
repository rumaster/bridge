# Backend Core OpenAPI

`openapi.json` is generated from `@bridge/backend` during `npm run build --workspace @bridge/backend`.

This directory owns the M0 C3 REST skeleton contract for backend core endpoints.
