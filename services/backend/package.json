{
  "name": "@bridge/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "find src test -name '*.mjs' -print0 | xargs -0 -n 1 node --check",
    "test": "node --test test/",
    "build": "find src -name '*.mjs' -print0 | xargs -0 -n 1 node --check"
  }
}
