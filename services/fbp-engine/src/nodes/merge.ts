/**
 * Слияние потоков (Ревизия 2026-07-15). Данных не трогает — это чистый
 * exec-синхронизатор: барьер держит исполнитель, пропуская узел дальше только
 * когда в него пришли ВСЕ подключённые exec-потоки (см. `core/executor.ts`).
 *
 * Нужен, потому что exec-выход может вести сразу в несколько узлов: без merge
 * ветки, разошедшиеся параллельно, продолжили бы граф каждая по разу.
 */
export const mergeNode = {
  type: "merge",

  validate() {
    // Динамические exec-входы in_1..in_N считает контракт C5 по рёбрам графа.
  },

  execute() {
    return { outputs: {} };
  },
};
