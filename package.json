{
  "name": "bridge",
  "version": "0.0.0",
  "private": true,
  "description": "MessengerBridge monorepo scaffold.",
  "type": "module",
  "engines": {
    "node": ">=20.18.1",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "clients/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present && node --check scripts/ci-placeholder.mjs && node --check scripts/workspace-command.mjs && node --check scripts/db-backup-restore.mjs && node --check scripts/db-migrate.mjs && node --check scripts/db-seed.mjs && node --check db/seeds/000001_m0_seed.mjs && find tests -name '*.mjs' -print0 | xargs -0 -n1 node --check",
    "test": "npm run test --workspaces --if-present && node --test tests/smoke/*.test.mjs",
    "build": "npm run build --workspaces --if-present",
    "smoke": "node --test tests/smoke/*.test.mjs",
    "test:unit": "npm test",
    "test:integration": "node --test tests/integration/*.test.mjs && npm run test:integration --workspace @bridge/integration-platform",
    "test:contract": "node --test tests/contract/*.test.mjs",
    "test:e2e": "node --test tests/e2e/*.test.mjs && npm run test:e2e --workspace @bridge/manager-workspace && npm run test:e2e --workspace @bridge/web-chat",
    "probe:edge:ws": "node experiments/edge-ws-load-probe.mjs",
    "db:migrate:up": "node scripts/db-migrate.mjs up",
    "db:migrate:down": "node scripts/db-migrate.mjs down",
    "db:migrate:rf:up": "node scripts/db-migrate.mjs up rf",
    "db:migrate:rf:down": "node scripts/db-migrate.mjs down rf",
    "db:seed": "node scripts/db-seed.mjs",
    "ci": "npm run lint && npm test && npm run build"
  },
  "msw": {
    "workerDirectory": [
      "apps/web-chat/public"
    ]
  },
  "devDependencies": {
    "node-pg-migrate": "^8.0.4",
    "pg": "^8.22.0",
    "testcontainers": "^11.14.0"
  },
  "overrides": {
    "multer": "^2.2.0",
    "uuid": "^11.1.1"
  }
}
